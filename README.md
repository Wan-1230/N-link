# N-Link

> 为尼康 Z 系列微单打造的 Android 连接与遥控伴侣 —— 稳定连接 · 极速传图 · 全功能遥控。

![Android](https://img.shields.io/badge/Android-10%2B-3DDC84?style=flat-square&logo=android)
![Kotlin](https://img.shields.io/badge/Kotlin-2.1.0-7F52FF?style=flat-square&logo=kotlin)
![minSdk](https://img.shields.io/badge/minSdk-29-00ACC1?style=flat-square)
![targetSdk](https://img.shields.io/badge/targetSdk-35-00897B?style=flat-square)
![Version](https://img.shields.io/badge/version-1.3.1-546E7A?style=flat-square)

N-Link 是一款面向尼康 Z 系列微单（Z50II / Z6III / Z8 / Z9 / Zf 等）的开源 Android 应用，打通 **连接 → 浏览 → 传输 → 遥控 → 监看** 的完整链路，以「永不断联」为核心卖点：

- **稳**：BLE 心跳保活 + 前台服务常驻，断线自动重连（< 3s）
- **快**：USB / WiFi 双高速通道，智能调度与自动回退
- **全**：照片传输 + 远程快门 + 实时取景 + 参数管理四合一
- **简**：一次配对、自动恢复、零上手成本

---

## ✨ 功能特性

### 三通道连接

| 通道 | 能力 |
|------|------|
| **BLE** | **辅助通道**：配对引导（免输入下发 WiFi 凭证）· 心跳保活 · GPS 注入 · 遥控快门 · 状态读取。采用尼康 BLE 配对协议与 Blowfish 加密 |
| **WiFi** | **主数据通道**：相机发现（mDNS `_ptp._tcp` / `_nikon._tcp` + 子网扫描），PTP/IP（ISO 15740）全量会话，承载实时取景与全分辨率传输 |
| **USB** | 有线优先通道，相机插入自动唤起，keepalive 保活，支持 USB Live View 与遥控；v1.2.0 完成全链路优化（PTP 事务层容器重组重建、USB 实时监看打通、相册加载与断连修复） |

> **通道定位**：BLE 负责「握手引导 + 状态注入 + 低功耗保活」，WiFi PTP/IP 与 USB 负责「搬运数据」。
> BLE 受 GATT 带宽限制，**不承载实时取景与大文件传输**；完整的能力边界、风险与参考来源见
> [docs/BLE-可行性评估.md](docs/BLE-可行性评估.md)（结论：可行，作为辅助通道保留并继续投入）。

### 照片浏览与传输

- 相机存储卡照片列表，18 张/页分页加载 + 缩略图实时预览，默认按拍摄时间降序（新拍的在前）
- 格式筛选（全部 / 照片 / 视频 / RAW / JPG），筛选无结果时自动兜底提示
- 单张 / 批量 / 筛选 / 全部下载，支持**跳过已下载**与剩余数量进度提示，传输队列支持暂停、恢复、取消
- **断点续传**：中断自动从断点恢复，失败自动重试并回退备用通道，传输去重；相册列表滑动节流，长列表不卡顿
- 归档至 `DCIM/N-Link`（MediaStore / Scoped Storage），「本地照片」Tab 随时回看，下载完成后触发媒体库扫描

### 远程遥控拍摄

- 两段式快门：半按对焦 → AF 收敛后自动释放快门，确保合焦
- **B 门**遥控（实时曝光计时）、**定时拍摄**（2s/5s/10s/自定义）、**间隔拍摄**（延时摄影）
- 曝光三要素（光圈 / 快门 / ISO）、白平衡、对焦模式、测光等参数实时读写
- 画面模式置于顶部下拉芯片快速切换，拍摄模式下拉分体按钮切换；更多动作长按弹出教程气泡

### 实时取景（Live View）

- 实时画面传输，端到端低延迟（目标 < 200ms @ WiFi 5GHz）
- 触摸对焦、手动对焦驱动、双指缩放
- 构图网格叠加（三分线等）、全屏沉浸体验、重力感应横竖屏

### 连接稳定性

- 前台连接服务（`connectedDevice|dataSync`）保活 + 开机自启 + WorkManager 健康检查
- 连接状态机：通道升级、失败回退、指数退避重连；WiFi STA 扫描失败原因可见化（权限 / 定位开关 / 系统限制逐项提示）
- 实时连接状态仪表盘与厂商后台策略适配

### 其他

- **快门次数查询**：机身 PTP 不支持时自动经 EXIF 解析（Digeeker 协议）
- 拍摄参数预设管理、传输完成通知、RAW 处理策略
- **应用内更新**：读取 GitHub Releases 元数据（仅提示正式版），解析夸克网盘回退链接写入本地缓存，更新弹窗纯文本呈现
- 支持与反馈页（问题反馈 / 交流群 / 捐赠入口）
- 黑白极简设计语言：DayNight 自适应主题、8px 圆角、PressEffect 按压反馈

### 版本增强速览

> 近 5 个版本的亮点速览，完整记录见 [更新日志](#-更新日志)。

| 版本 | 亮点 |
|------|------|
| **v1.3.1** | WiFi 连接页不再误显示 USB 提示 · USB 探测改为主动连接才提示 · 提示文案不再被截断（短文案 + 卡片完整指引） |
| **v1.3.0** | 第三方镜头不再被显示成 NIKKOR · 相册「✓ 已下载」角标 · 删除本地照片后状态自动恢复 · 拍摄后逐张即时入册 · USB 状态行修复 · 录像中监看断流修复 · 拍摄模式写入后回读确认 |
| **v1.2.2** | 监看心跳响应校验（修复偶发断连）· event 通道读超时 · WiFi STA 链路重构（对齐官方 SnapBridge 语义）· 相册快速翻页动画修复 |
| **v1.2.1** | 「不重复下载已下载照片」开关 · 剩余下载进度常驻 · 下载完成即时通知媒体库 · STA 扫描失败原因可见化 · 支持手动输入相机 IP |
| **v1.2.0** | USB 全链路优化（识别修复 / 相册秒开 / 实时监看打通）· 缩略图渐进加载 · 拍摄模式下拉切换 · 状态栏连接状态修正 |

---

## 🚀 快速开始

### 下载安装（普通用户）

| 渠道 | 说明 |
|------|------|
| **GitHub Releases** | [最新 Release](https://github.com/Wan-1230/N-link/releases/latest) 下载 APK（国内访问不稳定时用下方夸克网盘） |
| **夸克网盘** | [公开永久链接（免提取码）](https://pan.quark.cn/s/a04626b6e249)，目录 `/N-Link/releases/` 恒为最新版（历史版本归档于 `/N-Link/_archive-releases/`） |
| **应用内检查更新** | 启动时自动检查（只提示正式版），GitHub 不可达时自动回退到夸克网盘缓存链接 |

### 环境要求（开发者）

- Android Studio（JDK 17 + Android SDK 35）
- Android 10（API 29）及以上真机（建议支持 BLE 5.0 与 5GHz WiFi）

### 构建

```bash
cd N-Link
./gradlew :app:assembleDebug
```

调试 APK 输出：

```text
app/build/outputs/apk/debug/app-debug.apk
```

### 开发工具

无需真机即可联调协议栈：

```bash
# 在 PC 上启动模拟尼康相机（mDNS 广播 + PTP/IP 握手）
python tools/mock_nikon_ptp.py --advertise

# 探测相机 WiFi 的 PTP/IP 连通性（只读、逐包打印响应码）
python tools/ptp_probe.py --host 192.168.1.1
```

详细说明见 [tools/README.md](tools/README.md)。

---

## 🧱 技术栈

| 类别 | 选型 |
|------|------|
| 语言 / 构建 | Kotlin 2.1.0 · AGP 8.7.3 · Gradle Wrapper · KSP |
| UI | ViewBinding · Material Components 1.12 · ConstraintLayout · Navigation 2.8.5 |
| 架构 | MVVM + Repository，单向数据流（StateFlow / LiveData） |
| 依赖注入 | Hilt 2.53.1 |
| 异步 | Kotlin Coroutines 1.9.0 |
| 本地存储 | Room 2.6.1 · Gson |
| 后台任务 | WorkManager 2.10.0（hilt-work）· 前台服务 |
| 图片加载 | Coil 2.7.0 |
| 日志 | Timber |
| 测试 | JUnit4 · MockK 1.13.14 · coroutines-test |

## 🏗️ 项目结构

```text
app/src/main/java/com/nikonlink/app/
├── MainActivity.kt / NLinkApp.kt
├── device/        # 设备连接层：ble / ptp / usb / wifi（AP + STA）/ 连接状态机 / 前台服务
├── camera/        # 相机内容：相册与传输（gallery）/ 实时取景（liveview）/ 参数与快门次数（params）
├── capture/       # 远程遥控拍摄：两段式快门 / B 门 / 定时 / 间隔
├── settings/      # 设置页与支持反馈
└── shared/        # 通用组件（ui）、数据与 DI、应用内更新（update）
```

## 🧪 测试

```bash
./gradlew :app:testDebugUnitTest
```

覆盖 BLE 吹鱼加密、PTP 协议编解码、PTP/IP 会话状态机、相机文件格式判定等核心逻辑。

---

## 🔒 隐私与安全

- 纯本地通信：不依赖尼康云服务，默认不上传用户照片至第三方服务器
- 本地照片遵循 Android Scoped Storage 规范
- BLE 配对 Just Works + 应用层校验，WiFi 通道 WPA2/WPA3 加密
- `local.properties`、签名文件（`*.jks` / `.signing/`）、`.tmp_bili/` 素材、构建产物均不入库

---

## 📋 更新日志

> 从新到旧保留全部版本记录；应用内「检查更新」仅提示正式版。最新版下载见 [GitHub Releases](https://github.com/Wan-1230/N-link/releases/latest) 或上方夸克网盘。

### v1.3.1 — 2026-09-10

设备页连接文案修复：WiFi 场景不再出现 USB 专属提示，USB 提示不再被截断。

- **WiFi 场景误显示修复**：WiFi AP / STA 连接时设备页不再出现「未检测到 USB 相机」；USB 探测改为只在用户主动点连接时提示，开机自动探测保持静默
- **提示不再截断**：状态行只显示短文案（未检测到 USB 相机 / USB 连接失败 / USB 权限被拒绝），完整操作指引移入可换行的 USB 卡片
- **状态行收敛**：状态行渲染统一到单一出口，USB 文案仅在 USB 场景出现，切回 WiFi 页签立即恢复无线状态文案，杜绝文案残留

### v1.3.0 — 2026-09-10

七项体验优化：镜头信息显示、相册「已下载」状态可信化、拍摄后逐张即时刷新、USB 连接与状态、录像中的监看稳定性，以及拍摄模式远程切换。

- **镜头显示**：第三方镜头（适马 / 腾龙 / 蔡司等）不再被显示成 NIKKOR 前缀，只显示客观可得的规格（如 `18-50mm f/2.8`）；读不到规格时显示「未知镜头」，不再暴露内部镜头 ID
- **相册「已下载」角标**：相机照片页右下角为已下载照片显示「✓ 已下载」，一眼可辨
- **删除后状态自愈**：删除手机本地照片后，该张立即恢复为「未下载」——角标消失、在「未下载」筛选中重新出现、剩余进度计数回升；App 内删除与系统相册删除均成立
- **状态不再随时间丢失**：废弃按 30 天清理传输记录的旧策略，改为只以本地文件是否存在为准
- **拍摄后逐张即时入册**：启用尼康私有事件 `ObjectAddedInSDRAM`，相机拍完即抓取该张插入列表，单张与连拍都逐张即时出现
- **USB 状态与连接**：状态行改由 USB 连接状态驱动，修复「连上后一直显示正在建立 USB 通道...」；连接前清理上次残留的接口占用、失败后重试，减少反复插拔
- **录像监看**：修复视频模式 + 联动画面下开始录像后画面断开，停止录像自动恢复
- **拍摄模式远程切换**：写入后主动回读相机确认，分「切换成功 / 机型不支持（请用机身拨盘）/ 读取失败」三种结果明确提示

### v1.2.2 — 2026-09-09

稳定性专项：远端监看偶发断连修复 + WiFi STA 链路重构 + 相册快速翻页修复 + 对齐官方 SnapBridge 连接语义。

- **监看心跳校验**：心跳此前「只发不验」，相机侧已断开而 App 仍在等待；现在校验响应，超时即判定断连并走重连
- **事件通道不再阻塞**：event 通道加入读超时（45s）与心跳探测超时（1.5s），异常统一计为 `event_read_timeout` / `event_read_error` 并可恢复
- **WiFi STA 链路重构**：网络申请与绑网生命周期收敛到统一的 `StaNetworkRequester`，会话级持锁避免并发抢网，连接结束正确释放网络绑定
- **对齐 SnapBridge 语义**：连接失败自动重试（最多 5 次、间隔 300ms）、连接超时 30s、event 通道开启 `TCP_NODELAY` 降低指令延迟
- **相册翻页修复**：滑出屏幕的项立即取消残留动画并复位，快速翻页不再出现对勾错位与缩放残留；快速滚动期间暂停缩略图请求、停止后按可见区间补齐

### v1.2.1 — 2026-09-09

群友反馈专项修复 + WiFi STA 连接增强（原计划以 v1.3.0 发布，按用户决定改为小版本号）。

- **「不重复下载已下载照片」开关**：相机照片页标题右侧，开启后已下载照片立即隐藏，关闭后按原顺序恢复并回到原位置
- **剩余下载进度**：筛选行下方常驻「已下载 M/N · 剩余 K 张」，下载中实时显示整体百分比
- **导入即时可见**：照片下载完成后主动通知系统媒体库扫描，系统相册 / Google 相册无需手动刷新
- **缩略图合批刷新**：快速翻页不再卡住 / 花屏 / 堆叠
- **筛选兜底**：「未下载」筛选下全部下载完成时自动关闭筛选并提示，不再出现空列表
- **STA 扫描失败可见化**：显示已探测地址数、ARP 条目数、mDNS/NSD 响应情况；双无响应时提示「路由器可能过滤组播或开启了客户端隔离」
- **扫描增强**：总时长 12s → 18s，mDNS 每 4 秒重发、单次读超时 1s → 2.5s；新增手动输入相机 IP 直连

### v1.2.0 — 2026-09-07

USB 有线连接全链路优化 + 相册体验升级。

- **USB 相机识别修复**：机身自报 DeviceInfo Model 优先、PID 表兜底，不再出现「已连接但只识别一次」或型号错乱
- **相册加载提速**：元数据改用 MTP 批量接口（0x9805）一次往返拉取，数千张照片从分钟级降到秒级
- **USB 实时监看打通**：事务层容器重组重建，监看画面在 USB 通道下稳定出流
- **缩略图渐进加载**：小图先出、高清图后台静默升级；首次进入直接落在顶部，排序切换后回到第 1 页
- **预览左右滑动**：点开照片后可滑动切换上一张 / 下一张；「跳过已下载」开关迁移至刷新键左侧
- **拍摄模式下拉切换**：快门旁按钮改为「主体执行 + 右侧箭头弹菜单」，视频档自动隐藏间隔 / B 门按钮
- **远程模式切换校验**：0x500E 切换带读回确认，失败时按钮置灰并明确提示
- **遥控拍照不再断连**：快门期间监看帧错误宽限处理；全屏监看进出画面连续
- **状态栏修正**：通知状态与实际连接状态严格一致；本地相册二次打开缩略图秒出

### v1.1.0 — 2026-09-06

六大模块专项修复 + 长曝光与快门次数增强。

- **B 门全链路重构**：自动切 Bulb 档 → 多级触发指令（0x9207 / 0x100E / 0x100F 逐级兜底）→ 可靠收门，全程响应码可见
- **定时长曝光**：15 秒 ~ 5 分钟可选、剩余时长倒计时、随时提前收门；入口迁移至「间隔拍摄」按钮（点击执行 / 长按切换）
- **相册 Tab 修复**：三 Tab 收敛为单一数据源，点击后选中态与列表原子切换；下拉刷新后自动回顶部
- **长按滑动多选**：滑过即选、回滑即取消、到列表边缘自动滚动续选
- **双指缩放重构**：双指中点锚点、单指平移、边界回弹、双击复位；构图网格与画面像素级同步
- **拍摄模式双向同步**：机身拨盘切换 ≤500ms 跟随手机端
- **录像修复**：0xA004 语义修正、0x90C2 时序对齐 digiCamControl、录像不再强制依赖监看
- **USB 连接加固**：失败原因分类提示（权限 / 接口占用 / 会话失败 / 链路超时）、1s/2s/4s 退避重连、连接成功即预置取流管线
- **快门次数查询**：本地 MakerNotes 解析优先（离线私密），云端解析兜底，结果标注来源

### v1.0.2 — 2026-09-04

- **视频录制**：遥控页可直接录制，录制前自动开启监看；遇 NotLiveView / 相机忙时自动重试，失败原因中文化
- **拍摄模式远程切换**：遥控页「模式」栏可远程切换 P / S / A / M / Auto，全屏监看页模式标签同样可点
- **高清缩略图**：缩略图改用相机大图通道获取，照片下载完成后以原图重生成高清缩略图（长边 1024）
- **快门显示修正**：快门读取新增相机专有属性双源校验并按机身标称档位归一；设置快门时同步双写保证生效
- **对焦点位置修正**：点击对焦坐标统一处理取景黑边与双指缩放；对焦指令间增加生效等待
- **缓存升级**：缩略图解码上限 512 → 1024，旧小图缓存自动作废

### v1.0.1 — 2026-09-04

- **参数手动输入**：快门 / 光圈 / ISO 滚轮均支持直接键入（如 `1/250`、`f/2.8`、`ISO 800`），无此档位时自动切换到最接近的有效档并提示实际生效值
- **相册自动同步**：遥控拍摄完成后照片自动出现在相册；连拍 / 间隔拍摄合并为一次刷新
- **亮度直方图**：遥控拍摄页与全屏监看页均可开启，与取景画面同步刷新
- **快门上限扩展**：1/1000 → **1/4000**
- **档位显示修正**：不再出现 1/167 等相机实际不存在的档位名
- **支持入口**：设置页顶部新增「支持开发 · 请喝杯咖啡」

### v1.0.0 — 2026-09-03

N-Link 第一个正式版：通过 WiFi / USB 以 PTP 协议控制尼康相机，实现遥控拍摄、全屏监看、照片浏览与批量传输。

- **相册与传输**：已标记分区（连机后一键筛选、批量下载）、未下载筛选、EXIF 面板、分享副本、传输完成通知、应用内更新日志
- **体验优化**：照片按拍摄日期分组（组内全选 / 取消全选）、意见反馈直达 QQ 交流群、双击返回退出、监看横竖屏图标重绘
- **项目清理**：移除无引用的废弃资源与代码

### v0.1.4 — 2026-09-02

聚焦 WiFi STA 连接重构与有线实时监看启用修复，基于 392 行真机连接日志的 13 条根因逐一对症修复。

- **WiFi 连接重构**：连接入口收敛为单一连接循环（`WifiDirectConnector`），根治并发配对风暴；配对 / 会话恢复 / 状态机重连三条循环合一
- **重试节奏**：指数退避 1s → 15s 共 10 次（总窗口约 90s）；新增 `WifiEndpoint` 地址校验与归一化
- **网络监听**：连接期持有 `WifiLock`（息屏不被回收），每次尝试重新解析网络句柄；NSD 系统级 mDNS 与裸 Socket 并行互备
- **监看启动闭环重写**：重发 StartLiveView + 300ms 静默期 + 探帧确认（最多 6 轮），修复 USB 直连时监看必然失败自动停止
- **禁止条件可读**：启动前读取 `LiveViewProhibitCondition`，回放 / 菜单 / 大容量存储 / 电量不足时给出明确提示
- **停止校验**：EndLiveView 结果校验 + 曝光模式恢复；USB 判活收敛为通道优先级单点判定，僵死自动回落 WiFi

### v0.1.3 — 2026-08-31

- **修复**：全选下载重复下载、连接后相册不刷新、快门方向反转、光圈限位越界、检查更新（只提示正式版，预发布不推送）
- **相册排序模块**：支持最新优先 / 最旧优先 / 按文件类型排序，切换保持浏览位置
- **快门 1 秒分界显示**：<1s 显示分数（`1/250`、`1/2.5`），≥1s 显示秒数（`1s`、`1.3s`、`30s`）
- **长曝光档位**：快门档位表补齐 0.5s → 30s 标准档（共 46 档）

### v0.1.2 — 2026-08-22

- **WiFi STA 稳定性**：路由器断网恢复后自动重建 PTP 会话；PTP 断链恢复不再要求相机端重复确认
- **预览性能**：缩略图内存 LRU + 磁盘两级缓存，按需并发加载，解码移出主线程
- **下载速度**：分块 1MB → 4MB（失败自动降级）；流式写盘内存峰值降低约 50%；5GHz 优先 + 2.4GHz 回退
- **设置项落地**：下载画质、保存路径、连接偏好、自动下载新照片、5GHz 优先均实际生效
- **全链路日志**：链路级结构化打点、崩溃日志落盘、设置页可导出日志，PTP 错误码中文化

### v0.1.1 — 2026-08-16

- **项目更名为 N-Link**：应用名、通知、归档目录（`DCIM/N-Link`）、数据库统一更新
- **修复 STA 模式连接失败**：WiFi 扫描 / 历史 IP 探测 / PTP/IP 会话 socket 显式绑定当前 WiFi 网络，避免蜂窝数据抢占默认路由
- mDNS 组播扫描显式绑定 WiFi 网络，提升同网段相机发现成功率

### v0.1.0 — 2026-08-08

首个正式发布版本。

- 支持 WiFi AP / WiFi STA / USB 有线三种连接方式
- 实时监看、遥控拍摄、相机参数同步
- 相机照片与本地照片传输管理
- 白平衡、测光、对焦模式与尼康机身一一对应

---

## 🛣️ Roadmap

- [x] Phase 1：三通道连接 + 照片传输（MVP）
- [x] Phase 2：远程快门 / B 门 / 定时 / 间隔拍摄 + 参数读写
- [x] Phase 3：Live View 实时取景
- [x] v1.1.0：八项真机反馈修复 + 画面模式顶部下拉 + 教程气泡
- [x] v1.2.0：USB 连接与照片管理全链路优化（7 项）+ 三处断连修复 + 相册体验
- [x] v1.2.1：群友反馈优化（不重复下载开关 / 剩余下载进度 / 媒体库即时扫描 / STA 扫描增强）
- [x] v1.2.2：稳定性专项（远端监看断连修复 + WiFi STA 链路重构 + 相册翻页修复 + SnapBridge 语义对齐）
- [x] v1.3.0：七项体验优化（镜头显示 / 已下载角标 / 状态自愈 / USB 状态 / 逐张即时刷新 / 录像监看 / 模式回读）
- [x] v1.3.1：设备页连接文案修复（WiFi 场景误显示 USB 提示 / 提示文案截断）
- [ ] Phase 4：**AI 修图**（PRD 已完成，编辑器开发中）
- [ ] 监看色彩与 LUT（PRD 已完成，待开发）
- [ ] 更多机型适配与兼容性验证、iOS 规划

---

## 🤝 贡献

欢迎参与共建：

1. 遵循现有代码风格（Kotlin 官方风格、中文注释、统一 `Timber` 日志 Tag）
2. 核心协议改动请附带单元测试，真机行为变更请补充说明
3. 提交前确保 `:app:testDebugUnitTest` 通过

## 📄 许可与声明

本项目为**个人开源研究项目**，与尼康公司无任何关联，非尼康官方应用；`Nikon` 商标归其各自所有者所有。

> ⚠️ 仓库当前尚未附带开源许可证文件。在明确授权之前，请勿用于商业分发或二次发布，如需授权请联系作者。

*如果这个项目对你有帮助，欢迎反馈连接稳定性问题，帮助我们一起改进。*
