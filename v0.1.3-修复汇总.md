# N-Link v0.1.3 修复汇总

| 项 | 内容 |
| --- | --- |
| 基线 | `master` @ `74e136f`（v0.1.2 / versionCode 3） |
| 集成主干 | `dev-v0.1.3-multi-fix` @ `46ec1bd`（已合并全部 5 个分支） |
| PRD | `PRD-N-Link-v0.1.3-问题修复.md` |
| 完成时间 | 2026-08-30 |
| 总改动 | 14 个文件，+1617 / -271 |

## 分支总览

| 分支 | HEAD | 文件数 | 改动量 | 状态 |
| --- | --- | --- | --- | --- |
| `fix-album-download-dedup` | `f4b5965` | 2 | +401 / -99 | ✅ 已合并，待真机验证 |
| `fix-album-auto-refresh-on-connect` | `86a3308` | 2 | +101 / -22 | ✅ 已合并，待真机验证 |
| `fix-shutter-aperture-control` | `c53fa5a` | 4 | +445 / -147 | ✅ 已合并，待真机验证 |
| `feat-app-update-checker` | `30ea084` | 5 | +514 / -3 | ✅ 已合并，待网络验证 |
| `research-nikon-cloud-feasibility` | `008f443` | 1 | +156 | ✅ 已合并，结论：不可行 |
| `feat-album-sort-order` | `142a773` | 4 | +78 / -7 | ✅ 已合并（追加需求） |

**文件重叠检查：0 处**（已用 `comm -12` 对分支两两比对验证）
**合并方式**：5 个 merge commit，每分支 1 个提交、双亲保留；排序功能为后续追加、无分叉故快进；
集成后 tree 与工作区逐文件校验一致

## 构建产物

| 项 | 值 |
| --- | --- |
| 文件 | `N-Link-v0.1.3-debug.apk`（20 MB） |
| applicationId | `com.nikonlink.app.debug` |
| versionCode / versionName | `3` / `0.1.2-debug` |
| 构建结果 | `BUILD SUCCESSFUL in 4m 23s` |
| 构建时间 | 2026-08-30 10:02（含排序功能） |

**已在 dex 字符串池中核对，以下新符号均确认打入 APK**：

| 任务 | 核对通过的新符号 |
| --- | --- |
| T1 | `enqueueInternal`、`MIGRATION_1_2` |
| T2 | `cameraReady`、`onCameraReadyChanged` |
| T3 | `ShutterCatalog`、`ApertureCatalog`、`resolvePickerIndex`、`apertureOptions` |
| T4 | `shared/update/UpdateChecker`、`shared/update/ReleaseInfo`、`SemVer` |
| T6 | `sortNewestFirst`、`toggleSortOrder`、`albumSortNewestFirst`、`sortCamera`、`btnSort`、文案`最新在前`/`最早在前` |

## 验证状态

| 已做 | 未做 |
| --- | --- |
| ✅ `./gradlew :app:assembleDebug` 编译通过 | ❌ 真机功能验证（待测） |
| ✅ 51 个 Kotlin 文件结构检查（0 错误） | ❌ 单元测试 |
| ✅ 103 个 XML 文件格式校验（0 错误） | |
| ✅ 跨文件符号引用一致性核对 | |
| ✅ dex 层面核对新代码已打入 | |

> 编译需在放开沙箱的环境下执行（沙箱会阻止 Gradle 的 `native-platform.dll` 加载）。

---

## T1 `fix-album-download-dedup` —— 下载重复

**改动文件**
- `camera/gallery/TransferManager.kt`
- `shared/data/NLinkDatabase.kt`

**核心修复**：`processQueue()` 的 `repeat(2)` 重试循环没有成功终止条件，首轮成功后又跑第二轮，而首轮成功已删除临时文件，第二轮完整重下并二次写入 MediaStore → 产生 `xxx (1).JPG`。**网络正常时每张照片也必然下载落盘两次**。改为仅失败时重试一次。

**其余改动**
- 入队去重：队列内同 handle + 已传输批量查询（400 一批，一次入队 1~2 次查询，取代 N 次 IO）
- `queueMutex` 保护 `transferQueue`/`currentTask`/`currentJob`/`isPaused`，消除主线程与协程线程竞态
- `transfer_history.file_handle` 唯一索引 + `Migration(1,2)`（备份表 `transfer_history_bak_1` → 去重 → 建索引，失败有清表兑底）
- DAO 获取改用 Hilt `NLinkDatabaseEntryPoint`（`TransferManager` 由 AppModule 手动 new，无法追加构造参数）

**自测结论**

| AC | 场景 | 结论 |
| --- | --- | :-: |
| AC-1 | 全选 50 张下载一次，无 `(1)` 副本 | ⚠️ 需真机。代码保证：成功不再重试，`downloadVia` 只落盘一次 |
| AC-2 | 连点下载 5 次不膨胀 | ✅ 入队阶段被队列内 handle 拦掉 |
| AC-3 | 已下载再全选不入队 | ✅ 已传输 handle 不入队，`download_start` 不打点 |
| AC-4 | 中途断连重试 1 次 | ⚠️ 需真机 |
| AC-5 | 切 Tab/退后台不并发重复 | ⚠️ 需真机。Mutex 保证无并发 |
| AC-6 | 日志中 `download_start` ≤ `download_done` | ✅ 成功路径各 1 条 |

**回退点**：L1 去掉 `AppModule` 的 `.addMigrations(...)` 并把 `version` 退回 1（去重退化为应用层 `EXISTS` 查询，功能无损）；L2 整分支回滚。

---

## T2 `fix-album-auto-refresh-on-connect` —— 连接后相册刷新

**改动文件**
- `camera/gallery/TransferViewModel.kt`
- `camera/gallery/TransferFragment.kt`

**核心修复**：`MainActivity` 四 Fragment 常驻、切 Tab 只 `hide()/show()`，故 `TransferFragment.onViewCreated` 的 `fetchPhotos()` **整个 App 生命周期只跑一次**（冷启动、相机未连）。`TransferViewModel` 已暴露 `connectionState` 但 Fragment 从未 collect。

**改动**
- `cameraReady = combine(connectionState, usbState)`：三个来源都必须覆盖 —— `FULLY_CONNECTED`、`BLE_CONNECTED`、**`UsbConnectionState.CONNECTED`**（USB 通道不进连接状态机，`ConnectionManager:535` 只打日志，纯 USB 场景必须单独判定）
- `stateIn(..., Eagerly, ...)`：冷启动即拿初值，满足"已连相机时冷启动也自动加载"
- `onCameraReadyChanged()` 只在「未就绪→就绪」上升沿触发，避免持续重刷与切 Tab 重刷
- `loadPhotos(force)` 守卫：`AtomicBoolean` + `loadJob.cancel()`；手动刷新走 `force = true` 保证永远可打断
- `downloadAll()` 数据源由 `_photoList` 改为 `filteredPhotos`，与 `selectAllFiltered()` 口径一致（原会把视频也拉进下载队列）
- **手动刷新入口完全保留**：`btnRefresh` 与下拉刷新行为不变

**自测结论**

| AC | 场景 | 结论 |
| --- | :-: |
| AC-1 | 冷启动 → 连相机 → 切相册，自动加载 | ⚠️ 需真机 |
| AC-2 | 已连相机时冷启动，相册自动加载 | ✅ `Eagerly` 初值 + 上升沿判定 |
| AC-3 | 断开重连后自动刷新 | ✅ `lastReady` 回落再上升，触发 rising |
| AC-4 | 切走再切回不重复加载 | ✅ 上升沿只在 false→true 触发 |
| AC-5 | 手动刷新仍生效 | ✅ `force = true` 路径 |
| AC-6 | 自动加载中手动下拉不并发 | ✅ 守卫 + `loadJob.cancel()` |
| AC-7 | USB 有线连接也自动加载 | ✅ `usbState` 已纳入 `combine` |
| AC-8 | 自动下载开关关闭时也生效 | ✅ 不依赖 `scheduleAutoSync` |

**回退点**：删掉 Fragment 里 `cameraReady` 的 `collect` 块（1 处），自动刷新关闭、手动刷新不受影响。

---

## T3 `fix-shutter-aperture-control` —— 快门方向与光圈限位

**改动文件**
- `camera/params/CameraParameterManager.kt`
- `camera/params/CameraParamsViewModel.kt`
- `camera/liveview/LiveViewFragment.kt`
- `capture/RemoteFragment.kt`

**核心修复一（快门方向）**：滚轮把 `PROP_EXPOSURE_TIME` 的 raw 值（单位 **1/10000 秒**）直接当分母显示 —— `raw 40` 显示成 `1/40 s`，实为 **1/250s**。于是 raw 升序（变慢）与显示序列（1/10 → 1/4000，读起来变快）**方向完全相反**。附带导致 `indexOfFirst { it == current }` 恒匹配失败，滚轮每次跳回首项。

**核心修复二（光圈）**：`readLensInfo()` 其实**已经读到**镜头光圈上下限（`0xD0E5`/`0xD0E6`），但拼进 `lensName` 展示字符串后就丢弃了，`CameraInfo` 无任何结构化字段；`setAperture()` 也无 clamp 与回读校验。

**改动**
- 档位表收敛为 `ShutterCatalog` / `ApertureCatalog` 唯一真源，消除 LiveViewFragment / RemoteFragment / Manager **三处硬编码重复**
- `formatShutter()` 统一换算；`readShutterSpeed()` 复用同一函数，口径一致
- `resolvePickerIndex()` 改用 **raw 值**定位
- `CameraInfo` 增加 5 个镜头结构化字段；`apertureOptions()` 按当前焦距线性插值生成档位
- `setAperture()` clamp 到最近合法档 + 写入后回读比对；`setShutterSpeed()` 加 `coerceIn`
- 按 PRD Q3 决定：当前焦距**只在打开光圈滚轮时读一次**，不进 2 秒轮询

**自测结论**

| AC | 场景 | 结论 |
| :-: | --- | :-: |
| AC-1 | 刻度显示正确物理值 | ✅ 验算：`raw 10 → 1/1000`、`raw 40 → 1/250`、`raw 4000 → 0.4s` |
| AC-2 | 滚轮定位到当前值 | ✅ `raw 40` 精确定位 index 6 |
| AC-3 | 调节方向正确 | ⚠️ 需真机（逻辑已验证：`direction>0 → index+1 → raw 增大 → 曝光变长`） |
| AC-4 | 首尾限位 | ✅ `stepIndex.coerceIn` + `setShutterSpeed.coerceIn(10, 4000)` |
| AC-5 | 定焦 f/1.8 档位生成 | ✅ 验算得 `[1.8, 2, 2.8, 3.5, 4, 5.6, 8, 11, 16, 22]`，无 f/1.4 |
| AC-6 | 变焦 24-70 f/2.8-4 两端 | ⚠️ 需真机（插值验算 24mm→f/2.8、47mm→f/3.5、70mm→f/4.0） |
| AC-7 | 越界 clamp | ⚠️ 需真机（验算 `f/1.4 → f/1.8`） |
| AC-8 | 镜头未知回退 | ✅ 回退 f/1.4–f/22 通用 11 档 |
| AC-9 | 参数锁定 | ✅ 锁定即 `return false` |
| AC-10 | 两处口径一致 | ✅ 同源于 ViewModel |

**⚠️ 一处 PRD 偏离（需确认）**：PRD 写"≥1s 显示小数秒"，但 AC-1 要求 `raw 4000`（0.4s）显示 `0.4s`，二者矛盾。实现按**相机行业惯例取 ≥0.3s 显示小数秒**（`3200 → 0.3s`、`2500 → 1/4`）。

**回退点**：令 `apertureOptions()` 恒返回 `ApertureCatalog.FALLBACK` 即回到固定 11 档，快门修复仍生效。

---

## T4 `feat-app-update-checker` —— 检查更新

**改动文件**
- `shared/update/ReleaseInfo.kt`（新增）
- `shared/update/UpdateChecker.kt`（新增）
- `settings/SettingsFragment.kt`
- `res/layout/fragment_settings.xml`
- `shared/di/AppModule.kt`

**实现**
- 数据源：`GET /repos/Wan-1230/N-link/releases/latest`，`HttpURLConnection` + 已有 Gson，**未引入新依赖**，未动 `AndroidManifest`（`INTERNET` 已声明）
- `SemVer` 解析：支持 `v1.2.3` / `1.2` / `1.2.3-debug` / `1.2.3-beta.2`；预发布低于同号正式版；**剥离 `-debug` 构建后缀**（否则 debug 包 `0.1.2-debug` 会永远低于 tag `v0.1.2` 而误报更新）
- 8s 超时、1.5s 防抖、五态失败降级矩阵，**任何失败路径都不得误报"有更新"**
- 跳转优先取 apk 资产，无 apk 退化 release 页面；无浏览器时弹框展示 URL 供复制；**不做 APK 自下载安装**
- 设置页「通用设置」组新增入口（`rowAbout` 之前），手动触发；按 PRD Q4 决定不做启动静默检查
- 顺带接线：`.addMigrations(NLinkDatabase.MIGRATION_1_2)`（配合 T1 的 Room 升版）

**版本解析验算（离线实跑 24/24 通过）**

| 输入 | 输出 |
| --- | --- |
| `v1.2.3` / `1.2.3` / `v1.2` | `1.2.3` / `1.2.3` / `1.2.0` |
| `0.1.2-debug` | `0.1.2` |
| `1.2.3-beta.2` / `V2.0.0-RC1` | `1.2.3-beta.2` / `2.0.0-RC1` |
| `release-2026` / `1.2.3.4` / 空 | `null`（保守提示） |

比对：`0.1.2 vs v0.1.3`→有更新；`0.1.2 vs v0.1.2`→无；`0.1.3 vs v0.1.2`→无（不误报）；`0.1.2-debug vs v0.1.2`→无；`0.1.2 vs v0.1.10`→有（非字典序）；`1.0.0 vs v1.0.0-beta`→无；`1.0.0-beta vs v1.0.0`→有。

**自测结论**：AC-2/3/5/6/10/12 ✅；AC-1/4/7/8/9 ⚠️（需真实网络与已发布 release）；AC-11 ⚠️（默认不提示已实现，但开关未接入 `AppSettings` —— 该文件不在本分支可改范围）。

**回退点**：移除 `rowCheckUpdate` 的点击监听即可隐藏功能，UI 不报错。

---

## T5 `research-nikon-cloud-feasibility` —— 尼康云创评估

**改动文件**：`docs/尼康云创接入评估.md`（新增，156 行）

**结论：不可行，不建议实现。** 阻塞点为核心级，非工程量问题。

| # | 阻塞点 |
| --- | --- |
| B1 | 无公开 API —— 功能全部由 Web 前端内部调用，接口未对外暴露 |
| B2 | 无第三方 OAuth 客户端注册 —— 实测认证基于 Keycloak realm `user`，`client_id=c0001` 是尼康第一方 Web 客户端，复用属未授权访问 |
| B3 | 合规风险 —— 逆向复用第一方 client_id 违反尼康服务条款 |
| B4 | **数据同步范围不可介入** —— 影像传输是相机直连 Wi-Fi 上云，照片流不经过手机 App，第三方在链路上没有位置 |
| B5 | 凭据随时失效，无法作为产品功能交付 |
| B6 | 仅 Z6III/Z50II/Z5II/Zf/ZR 五款机型支持，与 N-Link 目标机型重合度低 |

**替代路径**：P1 云创网页入口（零风险只读跳转）/ P2 标准网盘直传（各平台有公开 API 与正规 OAuth）/ P2 申请尼康官方 SDK（仅桌面遥控，且官方不提供技术支持）/ P3 持续观望。

> P1 若落地需与 T4 串行改 `SettingsFragment.kt`。本版本**未实施**，故本分支零业务代码改动。

---

## T6 `feat-album-sort-order` —— 相册排序（追加需求）

**改动文件**
- `camera/gallery/TransferViewModel.kt`
- `camera/gallery/TransferFragment.kt`
- `shared/common/AppSettings.kt`
- `res/layout/fragment_transfer.xml`

**问题**：相机相册此前**没有任何排序**，直接沿用 `fetchPhotoList()` 返回的对象句柄顺序（旧→新），
新拍的照片落在列表最底部，要翻到底才看得到。全仓库唯一的排序出现在 `scheduleAutoSync()`
里的 `sortedByDescending { it.handle }`（仅用于挑最新 20 张自动下载），相册展示层从未排序。

**改动**
- `AppSettings.albumSortNewestFirst` 持久化偏好，**默认 `true`（最新在前）**
- 排序在**展示层** `displayedPhotos` 的 combine 中应用 —— 切换排序只重排，不重新拉取列表
- 分页回调 `onPage` 同样套用排序，避免"先正序渲染、加载完突然翻转"
- 顶部栏新增排序 chip（在刷新与多选之间），点击切换并 `scrollToPosition(0)` 回顶部
- **排序依据为 object handle**（递增即拍摄顺序递增），与 `scheduleAutoSync` 判定"最新照片"的口径一致
- **本地相册不参与**：其顺序由 MediaStore 按 `DATE_ADDED DESC` 返回，本身即最新在前，
  故排序入口在切到本地 tab 时隐藏（若强行按 handle 排序反而会打乱——本地 handle 是 `-id` 的负数）

**自测结论**

| 场景 | 结论 |
| --- | :-: |
| 打开相册，新拍照片排在最前 | ✅ 默认 `albumSortNewestFirst = true`，handle 降序 |
| 点击排序 chip 切换为正序 | ✅ 文案变为"最早在前"，列表翻转并回顶部 |
| 切换排序不重新拉取列表 | ✅ 仅 combine 重算，不触发 `fetchPhotoList` |
| 分页加载过程顺序一致 | ✅ `onPage` 同样排序 |
| 切到本地 tab | ✅ 排序 chip 隐藏，本地列表保持 MediaStore 顺序 |
| 重启 App 保留排序偏好 | ✅ 已持久化到 SharedPreferences |

**回退点**：把 `AppSettings.albumSortNewestFirst` 的默认值改回 `false` 即恢复旧行为；
或移除 `displayedPhotos` 中的 `sortCamera()` 调用。

---

## 合并建议

按 PRD 6.1，建议顺序：

```
git checkout dev-v0.1.3-multi-fix
git merge --no-ff fix-album-download-dedup          # 改动面最大
git merge --no-ff fix-shutter-aperture-control      # 次之
git merge --no-ff feat-app-update-checker
git merge --no-ff fix-album-auto-refresh-on-connect
git merge --no-ff research-nikon-cloud-feasibility
./gradlew :app:assembleDebug                        # 本机验证
```

由于文件零重叠，5 次 merge 均不会产生冲突。

## 测试清单（debug 包）

装好 `N-Link-v0.1.3-debug.apk` 后按优先级验证：

| 优先级 | 场景 | 期望 |
| --- | --- | --- |
| **P0** | 全选 50 张下载 | 系统相册 `N-Link` 目录文件数 == 50，**无 ` (1)` 副本** |
| **P0** | 打开快门滚轮 | 刻度显示 `1/1000` / `1/250` / `0.4s`（不再是 `1/10` / `1/40` / `1/4000`） |
| **P0** | 调快门方向 | 往"变慢"方向调，相机肩屏曝光时间变长；往"变快"方向则变短 |
| **P0** | 连接相机 → 切到相册 Tab | 无需手动操作，自动加载出照片 |
| P1 | 下载中连点下载 5 次 | 队列不膨胀，提示"均已在队列中或已下载" |
| P1 | 已下载后再全选下载 | 不入队，不发起传输 |
| **P0** | 打开相册 | 新拍的照片排在**最前面**，不用翻到底 |
| **P0** | 点顶部"最新在前" | 文案切为"最早在前"，列表翻转并回到顶部 |
| P1 | 光圈档位 | 定焦头不出现超出其最大光圈的档；变焦头转动焦段后最小 f 值随动 |
| P1 | 设置 → 检查更新 | 能正确报"已是最新"或弹出新版本对话框 |
| P1 | 排序偏好 | 切为"最早在前"后杀掉 App 重进，仍是"最早在前" |
| P1 | 切到本地照片 tab | 排序 chip 隐藏，本地列表顺序不受影响 |
| P2 | 断网后点检查更新 | 提示"无法连接服务器"，不崩溃 |
| P2 | 切 Tab 再切回相册 | 不重复加载（无 loading 闪烁） |
| P2 | 手动刷新 | 行为与之前一致，仍可打断自动加载 |

## 待办

1. **真机验证**（上表 P0 四项优先）
2. **确认 T3 的一处 PRD 偏离**：≥0.3s 显示小数秒（PRD 原文写 ≥1s，与 AC-1 矛盾）
3. **T4 的 AC-11 开关**未接入 `AppSettings`，如需"提示预发布版本"的开关需另开一改
4. 验证通过后发布 release：bump `versionCode` → 4、`versionName` → `0.1.3`，
   执行 `./gradlew :app:assembleRelease`（签名配置已在 `local.properties` 与 `.signing/`）
