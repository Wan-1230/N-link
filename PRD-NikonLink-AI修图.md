# NikonLink AI 修图功能 产品需求文档（PRD）

> 版本：v1.0
> 日期：2026-08-08
> 状态：Draft
> 平台：Android（API 29+）
> 上游文档：《PRD-NikonLink.md v1.0》（本 PRD 为其 Phase 4「体验优化与扩展」的子需求）

---

## 1. 背景与目标

### 1.1 背景

NikonLink 目前已完成「连接 → 浏览 → 传输 → 归档」的完整链路：

| 现有能力 | 现状 |
|---------|------|
| 相机连接 | BLE 保活 + WiFi PTP/IP + USB 有线三通道，USB 优先、互为兜底 |
| 照片浏览 | 相机存储卡列表（18/页分页加载）+ 缩略图实时预览 + 格式筛选（全部/照片/视频/RAW/JPG） |
| 照片传输 | 单张/批量下载、队列管理、断点续传、失败自动重试与通道回退、传输去重 |
| 本地归档 | 保存至 `DCIM/NikonLink`（MediaStore / Scoped Storage），「本地照片」Tab 可直接查询 |
| 图片浏览 | 全屏预览页 PreviewActivity（下载原图 / 拍摄信息 / 分享） |

**问题**：照片下载到手机后，用户链路到此中断——想要润色必须跳出 App，使用系统相册或第三方修图软件（Snapseed / Lightroom / 醒图等）。这带来三个损失：

1. **体验断层**：拍 → 传 → 修 → 发是摄影用户的一体化诉求，NikonLink 目前只覆盖前两步；
2. **留存损失**：传输完成后用户即离开 App，缺少高频二次使用场景；
3. **竞品差距**：SnapBridge 提供基础滤镜，影犀等第三方工具已内置 AI 修图入口，用户对「传完即可修」有明确预期。

### 1.2 目标

在 NikonLink 内新增 **AI 修图**能力，打通「拍摄 → 传输 → 修图 → 导出」的完整闭环：

1. 用户从**已下载照片 / 传输完成照片 / 本地相册**任一路径，一键进入 AI 修图页；
2. 提供**一键式 AI 增强**为核心卖点（零学习成本），辅以场景化优化（人像/风光）与精细调节（降噪/清晰度/色彩）；
3. 全部处理**默认本地完成、离线可用**，延续产品「不上传用户照片」的隐私承诺（PRD 5.3）；
4. 视觉与交互完全遵循现有黑白极简设计语言（DayNight、8px 圆角、PressEffect 点击反馈）。

### 1.3 成功指标（KPI）

| 指标 | 目标值 |
|------|--------|
| 传输完成后 24h 内使用修图功能的用户占比 | > 30% |
| 单张照片平均修图时长 | 30s ~ 3min（过短说明功能未被使用，过长说明易用性差） |
| 修图结果保存成功率 | > 99.5% |
| 修图功能 Crash 率 | < 0.1% |
| 修图功能次周留存（使用该功能的用户） | > 40% |

---

## 2. 目标用户与典型使用场景

### 2.1 目标用户

沿用主 PRD 1.3 定义：尼康 Z 系列微单用户、中级及以上摄影爱好者 / 职业摄影师。在修图场景下进一步细分：

| 细分画像 | 修图诉求 | 对应功能侧重 |
|---------|---------|-------------|
| 快拍快发用户（约 60%） | 传完立即发朋友圈/小红书，不想学参数 | 一键 AI 自动增强、滤镜预设 |
| 人像拍摄者 | 肤色、面部曝光、背景氛围 | 人像优化、色彩增强 |
| 风光/旅行拍摄者 | 通透度、天空色彩、暗部细节 | 风光优化、清晰度、降噪 |
| 严谨后期用户 | 只做微调，绝不允许 AI 改变原片基调 | 细粒度滑杆（0-100）、原图对比、另存不覆盖 |

### 2.2 典型使用场景

**场景 A：外拍即时出片（核心场景）**
> 用户外拍结束，照片经 WiFi/USB 自动传输到手机。传输完成通知弹出，用户点开「本地照片」Tab → 点选刚传的照片 → 进入预览页 → 点「AI 修图」→ 一键增强 → 长按对比原图满意 → 另存为新图 → 去系统相册分享。

**场景 B：相机卡直修（次核心场景）**
> 用户在相机照片 Tab 浏览存储卡，看到一张想修的图，尚未下载。在预览页点「AI 修图」，App 自动先下载原图再进入编辑器，无需用户手动操作两步。

**场景 C：历史照片回修**
> 用户几天前下载过一批照片，再次打开 App 进入「本地照片」Tab，对历史照片重新修图。无需相机连接，全程离线。

**场景 D：批量快速出片（P2）**
> 用户选中一组同场景照片，应用同一个预设/一键增强参数，批量导出。

---

## 3. 功能范围

### 3.1 功能范围（In Scope）

- AI 修图编辑器页面（单张编辑）与三个入口（预览页 / 本地相册 / 传输完成通知）
- AI 自动增强、人像优化、风光优化、降噪、清晰度、色彩增强、一键修复、滤镜/预设
- 前后对比、撤销/重做、滑杆强度调节
- 保存导出：另存为新图（默认）/ 覆盖保存（二次确认）
- 与 MediaStore 本地相册、传输流程、预览页的集成

### 3.2 非功能范围（Out of Scope，本版本不做）

| 项目 | 原因 | 后续考虑 |
|------|------|---------|
| RAW/NEF 直接编辑 | NEF 解码依赖私有格式，风险高（见 §10） | v2 评估 DNG 转换路径 |
| 视频修图/调色 | 处理复杂度与存储压力过大 | 远期 |
| 裁剪/旋转/污点去除等几何与手动工具 | 本期聚焦 AI 增强，避免范围膨胀 | v1.1 补充 |
| 人像「医美级」美颜（瘦脸/大眼） | 与专业摄影工具定位不符 | 待确认（见 §11） |
| 云端 AI 处理 | 与「不上传用户数据」隐私承诺冲突 | P2 待决策 |
| 多图合成 / HDR 合并 | 超出单张编辑范畴 | 远期 |
| 修图参数跨设备同步 | 无账号体系 | 远期 |

---

## 4. 核心功能需求

> 优先级说明：**P0** = 首版必须；**P1** = 首版后一个迭代内交付；**P2** = 视数据与资源排期。

### 4.1 AI 自动增强（P0）

| 需求项 | 说明 |
|--------|------|
| 一键增强 | 单个按钮触发，自动分析曝光/白平衡/对比度/色彩并输出增强结果 |
| 强度可调 | 增强结果附带 0-100 强度滑杆，0 = 原图，100 = 完整增强 |
| 场景感知 | 引擎内部自动识别场景类型（人像/风光/夜景/食物等）并选择策略，用户无感 |
| 幂等性 | 对同一张图重复点击结果一致（固定种子/确定性推理） |
| 处理中反馈 | 进度提示 + 可取消；处理失败给出中文原因与「重试」按钮 |

### 4.2 场景优化（P1）

| 功能 | 说明 |
|------|------|
| 人像优化 | 面部识别后优化：肤色提亮均匀化、面部曝光补偿、轻度磨皮（保留皮肤纹理）、发丝/眼部清晰度。未检测到人脸时提示「未识别人脸」并允许手动应用全局策略 |
| 风光优化 | 天空/植被分区增强、通透度提升、暗部细节恢复、地平线水平校正不做（几何工具不在本期范围） |

两者均带 0-100 强度滑杆，可与 AI 自动增强叠加（叠加顺序：场景优化 → 自动增强 → 细节调节 → 滤镜）。

### 4.3 细节调节（P1）

| 功能 | 说明 | 滑杆范围 |
|------|------|---------|
| 降噪 | 亮度降噪 + 色彩降噪，夜景/高 ISO 场景重点优化；强度越高细节损失越多，需在预览层实时可见 | 0-100，默认 0 |
| 清晰度 | 局部对比度增强（Clarity 式），避免全局锐化的白边 | 0-100，默认 0 |
| 细节重建 | 基于 Real-ESRGAN_x4plus 的可选开关，对软焦/低分辨率图做 AI 细节重建；默认关闭，需高级模型包 | 开关 + 0-100 强度 |
| 色彩增强 | 自然饱和度优先（保护肤色不过饱和），附加饱和度微调 | 0-100，默认 0 |

基础调节项（亮度/对比度/色温/高光/阴影，P0 随编辑器框架提供，非 AI 能力，用常规图像处理实现）。

**渲染模式**：降噪、细节重建等大模型功能采用「预览层实时（长边 1080p）+ 导出时全分辨率精细渲染」双模式，耗时指标见 §9.1。

### 4.4 一键修复（P1）

| 需求项 | 说明 |
|--------|------|
| 功能定义 | 针对常见拍摄缺陷的组合修复：欠曝/过曝拉回、灰蒙蒙去雾、白平衡纠偏、轻微噪点清理 |
| 与「AI 自动增强」的区别 | 自动增强面向「正常照片更好看」；一键修复面向「有缺陷的照片先救回来」。引擎内部为不同策略集，UI 为两个独立按钮 |
| 交互 | 点击后给出前后对比动画（0.6s 擦除式过渡），可撤销 |

### 4.5 滤镜 / 预设（P0 基础 + P1 扩展）

| 需求项 | 优先级 | 说明 |
|--------|--------|------|
| 内置滤镜 | P0 | 首版 8-12 款：原图、黑白、胶片、日系、青橙、复古、冷调、暖调、高对比、低饱和；每款以当前照片的实时小图缩略展示 |
| 滤镜强度 | P0 | 应用后 0-100 强度滑杆 |
| 预设组合 | P1 | 滤镜 + 细节参数的组合预设（如「直出优化 = 自动增强 60 + 清晰度 20」），支持另存为自定义预设（本地存储，Room） |
| 滤镜缩略图加载 | P0 | 基于降采样图（≤512px）实时生成，全部缩略图渲染总耗时 < 1.5s |

### 4.6 前后对比（P0）

| 需求项 | 说明 |
|--------|------|
| 长按对比 | 按住画布任意位置显示原图，松开恢复效果图（零切换成本） |
| 分屏对比 | 对比按钮触发左右分屏，分割线可拖动 |
| 缩放同步 | 对比时保持当前缩放与平移位置不变 |

### 4.7 保存与导出（P0）

| 需求项 | 优先级 | 说明 |
|--------|--------|------|
| 另存为新图 | P0（默认） | 输出至 `DCIM/NikonLink/Edited/`，文件名 `{原名}_EDITED_{yyyyMMdd_HHmmss}.jpg`，不触碰原图 |
| 导出画质选择 | P0 | 高质量 JPEG（q=95）/ 原始尺寸 PNG 二选一，默认高质量 JPEG |
| 快速导出 | P1 | 大模型功能（降噪/细节重建）提供「快速导出（长边 4K）」选项，将全分辨率精细渲染等待从数十秒缩短至 < 10s |
| EXIF 保留 | P0 | 复制原图 EXIF（拍摄参数、时间、GPS），追加 `Software=NikonLink Edit vX` 标记；GPS 信息遵循原图，不新增采集 |
| 覆盖保存 | P1 | 直接替换原文件；仅对本 App 创建的文件可直接覆盖，其他来源文件触发系统 SAF 授权（`MediaStore.createWriteRequest`），用户拒绝时降级为另存；覆盖前系统自动备份原图至 `DCIM/NikonLink/.backup/`（隐藏目录，保留 30 天） |
| 保存后反馈 | P0 | Toast「已保存」+ 提供「去相册查看」快捷跳转；返回相册页自动刷新列表 |
| 未保存退出拦截 | P0 | 有未保存修改时返回键弹出确认对话框：保存 / 不保存退出 / 取消 |

### 4.8 编辑状态管理（P0）

| 需求项 | 优先级 | 说明 |
|--------|--------|------|
| 撤销/重做 | P0 | 栈式历史，至少 20 步；每步记录为参数变更而非位图快照（内存友好） |
| 参数化编辑 | P0 | 所有效果以参数集（EditParams）描述，预览层实时合成；仅在导出时对全分辨率图执行一次完整管线 |
| 编辑会话恢复 | P2 | 进程被杀后重新打开同一张图，询问是否恢复上次未保存参数 |

---

## 5. 用户流程与页面结构

### 5.1 入口设计

```
入口①  相册 Tab → 本地照片 → 点选照片 → PreviewActivity → 底部新增「AI 修图」按钮
入口②  相册 Tab → 相机照片 → PreviewActivity → 点「AI 修图」→ 未下载则自动下载 → 进入编辑器
入口③  传输完成通知 → Action「修图」（P1）
入口④  本地照片网格长按多选 → 批量操作栏新增「AI 修图」（P2，配合批量编辑）
```

入口可见性规则：
- 仅对 JPEG/PNG 图片显示「AI 修图」按钮；RAW/NEF 与视频置灰并提示「暂不支持该格式」（见 §11 Q3）
- 相机照片未下载时按钮文案为「下载并修图」

### 5.2 用户主流程

```
选择照片 → [相机照片? 自动下载] → 编辑器加载(降采样预览)
   → 一键增强 / 选择场景 / 调节滑杆 / 套用滤镜
   → 长按对比原图 → 不满意: 撤销 / 重置
   → 点「保存」→ 选择另存(默认)/覆盖(P1) → 全分辨率渲染导出
   → Toast 成功 + 返回相册页自动刷新
```

### 5.3 编辑器页面结构（EditActivity）

遵循黑白极简设计语言：画布区纯黑背景，工具区跟随 DayNight 主题，所有可点元素应用 `pressEffect()`。

```
┌────────────────────────────────────────┐
│ ← 返回   文件名.NEF.JPG   ⟲ ⟳  ▤对比  ✓保存 │  ← 顶部悬浮工具栏
├────────────────────────────────────────┤
│                                        │
│           图像画布                      │  ← 双指缩放 / 双击100% / 长按对比原图
│        (可缩放、可平移)                 │
│                                        │
├────────────────────────────────────────┤
│ [一键] [场景] [细节] [滤镜]              │  ← 工具分类 Tab（横向）
│ ┌──────┐ ┌──────┐ ┌──────┐            │
│ │自动增强│ │一键修复│ │      │            │  ← 功能卡片（横向滚动）
│ └──────┘ └──────┘ └──────┘            │
│  强度 ▁▁▁▁▂▂▃▃▃▃▃▃▃▃▃▃▃▃ 62           │  ← 当前选中功能的强度滑杆
└────────────────────────────────────────┘
```

| Tab | 内容 |
|-----|------|
| 一键 | AI 自动增强、一键修复（大按钮卡片，点击即应用，再点强度滑杆出现） |
| 场景 | 人像优化、风光优化（卡片 + 强度滑杆） |
| 细节 | 降噪、清晰度、色彩增强 + 基础调节（亮度/对比度/色温/高光/阴影） |
| 滤镜 | 横向缩略图列表（实时渲染）+ 滤镜强度滑杆 |

### 5.4 保存对话框

```
┌─────────────────────────┐
│        保存修图结果        │
│  ○ 另存为新图（推荐）      │
│     DCIM/NikonLink/Edited│
│  ○ 覆盖原图               │
│     (原图将备份至隐藏目录)  │
│  画质: [高质量JPEG ▾]     │
│      [ 取消 ]  [ 保存 ]  │
└─────────────────────────┘
```

---

## 6. 与现有模块的集成方式

### 6.1 模块划分（新增）

```
app/src/main/java/com/nikonlink/app/
├── core/
│   └── imaging/                    # 新增：图像处理引擎层
│       ├── EditEngine.kt           # 管线调度：参数集 → 位图（含分块推理）
│       ├── AiEnhancer.kt           # AI 推理封装（LiteRT，tile 调度）
│       ├── ModelDelivery.kt        # 模型包按需下载/SHA-256 校验/版本管理
│       └── FilterLut.kt            # 滤镜 LUT/参数定义
├── feature/
│   └── edit/                       # 新增：AI 修图功能模块
│       ├── EditActivity.kt         # 编辑器页面
│       ├── EditViewModel.kt        # 状态管理（EditParams/历史栈/导出）
│       ├── EditPresetManager.kt    # 自定义预设（Room 存储，P1）
│       └── preset/                 # 预设数据模型
└── data/
    └── repository/                 # TransferRepository 不动；新增 EditRepository（P1，记录编辑历史）
```

### 6.2 与传输模块（TransferManager）集成

- 入口②「相机照片直接修图」复用 `downloadPhoto(file)`：成功后拿到 `TransferResult.Success(path)`，以该 MediaStore URI 打开编辑器；失败复用现有中文化错误提示（`TransferResult.Failed(reason)`）
- 修图不占用传输队列：编辑器打开期间传输队列正常运行，但同一文件若正在队列中下载，编辑器入口显示「下载中」禁用态
- 修图导出**不走** `saveFileToMediaStore` 的传输去重逻辑（`transferRepository.recordTransfer`），编辑产物是全新文件，不注册为「已传输」，避免干扰相机文件去重

### 6.3 与相册/浏览模块集成

- 导出后通过 `MediaStore` 写入即自动出现在系统相册；App 内「本地照片」Tab 查询条件为 `RELATIVE_PATH LIKE '%NikonLink%'`，`DCIM/NikonLink/Edited/` 天然命中，返回列表后调用 `fetchLocalPhotos()` 刷新即可
- PreviewActivity 底部操作栏新增「AI 修图」按钮（与下载/EXIF/分享并列）；`btnMore` 菜单同步增加该条目
- 网格页 `item_photo_grid.xml` 上编辑产物缩略图右下角加单色小角标「已修」（P1，灰度图标，符合黑白规范）

### 6.4 与通知/服务集成

- 传输完成通知（ConnectionService 现有通知渠道）增加 Action「修图」，点击携带 MediaStore URI 直达 EditActivity（P1）
- 全分辨率渲染导出若用户中途切后台：渲染在协程中执行且绑定 Application 级 scope（不绑定 Activity），返回后展示结果；不引入 ForegroundService（单次导出耗时短，避免通知打扰）

### 6.5 与设计体系集成

- 颜色全部取自 `values/colors.xml` / `values-night/colors.xml` 现有灰阶体系，不新增彩色主色
- 唯一例外：滤镜缩略图本身是彩色的（内容图，不属于 UI 装饰色）
- 转场沿用 `NlWindowAnim` 右推入；点击反馈统一 `pressEffect()`

---

## 7. 优先级总览

| 功能 | 优先级 | 说明 |
|------|--------|------|
| 编辑器框架：画布/缩放/参数化管线/撤销重做 | P0 | 一切的地基 |
| 入口①②（预览页进入、相机照片下载并修图） | P0 | 主路径 |
| 基础调节（亮度/对比度/色温/高光/阴影） | P0 | 非 AI，常规实现，保底可用性 |
| AI 自动增强（一键 + 强度） | P0 | 核心卖点 |
| 滤镜/预设（内置 8-12 款 + 强度） | P0 | 零成本出片感 |
| 前后对比（长按 + 分屏） | P0 | 信任感关键 |
| 另存为新图 + EXIF 保留 + 画质选择 | P0 | 安全导出 |
| 未保存退出拦截 | P0 | 防误操作 |
| 人像优化 / 风光优化 | P1 | 场景化差异点 |
| 降噪 / 细节重建 / 清晰度 / 色彩增强 | P1 | 精细调节（大模型精细渲染） |
| 一键修复 | P1 | 废片拯救 |
| 覆盖保存（含备份与 SAF 处理） | P1 | 高级诉求 |
| 自定义预设（Room 存储） | P1 | 重度用户 |
| 入口③（通知直达） | P1 | 转化提效 |
| 「已修」角标 | P1 | 状态可感知 |
| 入口④ + 批量编辑导出 | P2 | 效率工具 |
| 编辑会话恢复 | P2 | 长尾体验 |
| RAW/NEF 编辑、HEIC 输入、云端增强 | P2/待确认 | 见 §11 |

---

## 8. 技术与平台约束

### 8.1 本地处理 vs 云端处理

| 维度 | 本地处理（选定方案） | 云端处理（不选） |
|------|--------------------|-----------------|
| 隐私 | ✅ 照片不离开设备，与主 PRD 5.3「不上传任何用户数据」一致 | ❌ 需上传照片，破坏产品隐私承诺，需额外合规建设 |
| 离线可用 | ✅ 外拍无网场景完全可用（核心场景 A 刚需） | ❌ 依赖网络 |
| 成本 | 一次性模型投入，无按量成本 | 按调用计费，长期成本不可控 |
| 效果上限 | 受端侧模型规模限制 | 可上大模型，效果更好 |
| 结论 | **P0/P1 全部功能本地实现** | 云端仅作为 P2 可选项，且需先解决隐私授权方案（§11 Q4） |

### 8.2 端侧 AI 技术路线（方案 D：性能优先）

**推理框架**：LiteRT (TFLite) 优先；加速路径 GPU Delegate → NNAPI/NPU → CPU 多线程，逐级回退。

**模型清单**（全部 INT8/fp16 量化，合计约 100-120MB，分发策略见 §8.5）：

| 能力 | 模型 | 参数量 | 体积（量化后） | 分发 | 对应功能 |
|------|------|--------|---------------|------|---------|
| 一键增强 | Image-Adaptive 3D LUT（CVPR 2022） | — | ~4 MB | 内置 | AI 自动增强 |
| 场景识别 | EfficientNet-Lite2（Places365 微调） | ~6M | ~6 MB | 内置 | 自动增强策略路由 |
| 降噪 | NAFNet-width64（SIDD） | ~27M | ~28 MB | 按需下载 | 降噪 |
| 细节重建 | Real-ESRGAN_x4plus | ~16M | ~17 MB | 按需下载 | 细节重建（清晰度增强选项） |
| 人脸检测 | SCRFD-10M | ~10M | ~17 MB | 按需下载 | 人像优化前置 |
| 人脸分区 | BiSeNet 人脸解析 | ~13M | ~26 MB（fp16） | 按需下载 | 人像皮肤/眉毛/唇部分区 |
| 低光增强 | RetinexFormer | ~1-3M | ~5-12 MB | 按需下载 | 夜景增强子策略 |
| 去雾 | MPRA-Net / DehazeFormer-L | ~2-4M | ~4 MB | 按需下载 | 一键修复去雾 |
| 基础调节/滤镜 | OpenGL ES + 着色器 / LUT | — | 0 MB | 内置 | 实时预览层逐帧合成 |

**关键约束**：
- 人脸检测/分区**不引入 Google ML Kit**，避免 GMS 依赖，保证华为等无 GMS 设备可用
- 大图解码：BitmapFactory 降采样预览 + 导出时全分辨率分块处理；45MP 全分辨率位图约 180MB（ARGB_8888），禁止同时持有原图+结果两份全尺寸位图
- 所有大模型的全分辨率推理必须走分块（tile）推理（§8.4）

### 8.3 平台与权限

| 项 | 约束 |
|----|------|
| 最低版本 | Android 10（API 29），与主工程一致 |
| 基准设备 | 骁龙 7 系中端机（性能指标按此标定），向下需保证 4GB RAM 设备不崩溃 |
| 新增权限 | **无**。读取走已有 `READ_MEDIA_IMAGES`；写入 `DCIM/` 走 MediaStore 免权限；覆盖他应用文件走 SAF 临时授权（不计入清单权限） |
| 网络 | 修图处理过程本身零网络通信；唯一网络行为是「高级模型包下载」（需用户显式点击并提示流量）。云端增强 P2 待决策 |
| 图片格式 | 输入：JPEG（P0）、PNG（P0）、HEIC（P2，依赖系统 ImageDecoder）；NEF/RAW 本期不支持。输出：JPEG/PNG |
| APK 体积 | APK 增量 ≤ 20MB（内置模型 ~15MB + LiteRT 框架）；高级模型包 ~100MB 按需下载（§8.5） |

### 8.4 线程、内存与分块推理

- 推理与渲染全部在 `Dispatchers.Default` / 专用渲染线程，UI 线程只做位图上传与合成结果交换
- **分块（tile）推理**：大模型（NAFNet / Real-ESRGAN / BiSeNet 等）全分辨率处理必须分块；tile 尺寸按设备 RAM 动态选择：8GB+ → 256px，6GB → 192px，4GB → 128px；相邻块预留 16px 重叠边距消除接缝；单块内存超预算时自动降档重试
- 编辑器峰值内存预算：基准设备（8GB RAM）上编辑 45MP 图时进程增量 ≤ 500MB；收到 `onTrimMemory(TRIM_MEMORY_RUNNING_CRITICAL)` 时主动释放全分辨率缓存，仅保留预览层
- 所有长任务（推理/导出/精细渲染）支持协程取消；页面销毁必须同步回收 GPU/推理会话，防止泄漏
- 精细渲染支持转后台执行（绑定 Application 级 scope）与进度回调，UI 层显示剩余时间预估

### 8.5 模型交付策略

```
APK 内置（~15MB）              按需下载（~100MB）
├─ EfficientNet-Lite2 场景识别  ├─ NAFNet-w64 降噪
├─ Image-Adaptive LUT 增强     ├─ Real-ESRGAN_x4plus 细节重建
└─ LiteRT 推理框架              ├─ SCRFD-10M 人脸检测
                               ├─ BiSeNet 人脸分区
                               ├─ RetinexFormer 低光
                               └─ MPRA-Net 去雾
```

- 开箱即用：一键增强、基础调节、滤镜安装后立即可用，不依赖任何下载
- 首次使用需高级模型的功能时提示「下载高清修图组件（约 100MB，建议 WiFi）」，支持断点续传、进度显示、WiFi 下后台下载
- 模型存放于 App 私有目录，Room 记录版本号与 SHA-256，加载前强制校验完整性
- **降级链**：未下载/下载失败 → 该功能回退传统算法实现（降噪 → NLM、细节 → 局部对比度、人像 → 禁用并说明原因），基础修图流程完全不受影响
- 模型更新按版本号整包替换，不保留旧版本

---

## 9. 非功能性要求

### 9.1 性能指标

| 指标 | 目标值 | 测量条件 |
|------|--------|---------|
| 编辑器打开耗时 | < 1s（点击到预览可交互） | 24MP JPEG，骁龙 7 系 |
| 预览层调节延迟 | < 100ms | 滑杆拖动到画面更新，≤1080p 预览层 |
| 预览层 AI 渲染 | < 1s | 大模型（降噪/人像）在长边 1080p 降采样层运行 |
| 滤镜缩略图全量渲染 | < 1.5s | 10 款滤镜，512px 缩略 |
| AI 自动增强（全分辨率导出） | < 6s | 24MP；LUT + 轻量管线，不含大模型推理 |
| 精细渲染-降噪（全分辨率导出） | 15-30s（NPU 可用）/ 30-60s（CPU 兜底） | 24MP，tile=256，骁龙 7 系中端机 |
| 精细渲染-细节重建 | 20-40s | 同上 |
| 快速导出（长边 4K） | < 10s | 大模型功能的替代路径 |
| 保存写盘 | < 2s | 在上述推理/渲染耗时之外，24MP JPEG q95 |
| 45MP 大图 | 全流程不 OOM | 基准设备 8GB RAM，tile=256 |

### 9.2 质量要求

- 导出图与原图分辨率一致（不默认缩图），EXIF 完整
- AI 增强不得引入明显伪影（色带、油画感、肤色蜡像感）；以内部盲测对比 Snapseed 自动增强，「优于或持平」得票率 ≥ 70% 方可发版
- 滤镜风格一致性：同一滤镜在预览层与全分辨率导出结果视觉一致（允许抗锯齿级别差异）

### 9.3 隐私与合规

- 照片处理全程本地、零网络传输（P0/P1）；模型包下载通道仅传输模型文件，不含任何用户图像
- 不采集、不上报照片内容；埋点仅记录功能使用行为（如「点击一键增强」），不含图片数据与文件路径
- EXIF GPS 信息仅做保留或剥离（用户可选导出时去除位置信息，P1），绝不新增位置采集
- 覆盖保存前必须有备份与明确的用户确认，符合「用户数据不可静默丢失」原则

### 9.4 可靠性：失败重试与异常处理

| 场景 | 处理策略 |
|------|---------|
| 模型加载失败 | 降级：对应功能回退传统算法（降噪 → NLM、细节 → 局部对比度、人像 → 提示下载组件），基础调节与滤镜不受影响；Timber 记录 `EditEngine` 标签错误 |
| 模型下载失败/存储不足 | 断点续传重试；失败后中文化提示原因；对应功能保持传统算法兜底，不阻断使用 |
| 精细渲染超时（>120s） | 允许转后台继续执行；提供「取消并改用快速导出（长边 4K）」降级选项 |
| 推理失败/超时（>30s） | 自动重试 1 次，仍失败提示「处理失败，请重试或降低图片尺寸」，保留用户已做的基础调节 |
| 内存不足 | 预览层降一档采样率重试；导出失败时建议导出高质量 JPEG 而非 PNG |
| 写盘失败（空间不足/权限拒绝） | 中文化提示 + 明确原因；SAF 授权被拒时自动降级为另存 |
| 编辑中 App 被杀 | 原图零风险（参数化编辑不写原图）；重新进入提示可重新编辑 |
| 日志规范 | 统一 `Timber.tag("EditEngine"/"EditVM")`；AI 推理耗时、模型版本、失败原因必打点（遵循项目「注释/日志标注来源」规范） |

---

## 10. 验收标准与里程碑

### 10.1 验收标准（P0 发版门槛）

**功能验收**
- [ ] 三个入口（预览页本地照片/相机照片下载并修图）均可进入编辑器，RAW/视频/未下载态的按钮状态正确
- [ ] 一键增强在 24MP 图上 < 6s 完成，强度滑杆 0/50/100 结果肉眼可区分
- [ ] 高级模型包按需下载、断点续传、SHA-256 校验正常；下载失败/拒绝后功能降级为传统算法且不崩溃
- [ ] 24MP 全分辨率降噪精细渲染在承诺耗时区间内完成，全程有显式进度与剩余时间预估，可取消、可转后台
- [ ] 10 款滤镜全部可用，缩略图实时渲染 < 1.5s
- [ ] 长按对比与分屏对比正常，缩放位置保持
- [ ] 撤销/重做 ≥ 20 步，重置一键还原
- [ ] 另存文件出现在 `DCIM/NikonLink/Edited/`，出现在系统相册与 App「本地照片」Tab
- [ ] 导出图 EXIF 含原拍摄参数，分辨率与原图一致
- [ ] 有未保存修改时返回必弹确认框；「不保存」后原图逐字节不变（md5 校验）

**稳定性验收**
- [ ] 连续编辑 50 张（混合 24MP/45MP）无 OOM、无崩溃、无内存持续上涨
- [ ] 编辑中拔线（USB 通道）、断 WiFi、来电、切后台 10 分钟，返回后编辑状态完整
- [ ] Monkey 测试编辑器页面 10 万次事件零崩溃

**合规验收**
- [ ] 抓包确认修图处理过程零网络通信；模型下载请求仅传输模型文件（HTTPS + 内容抽查）
- [ ] 覆盖保存（P1）前备份文件存在且可还原

### 10.2 里程碑规划

| 里程碑 | 周期 | 交付内容 |
|--------|------|---------|
| M1 编辑器框架 | 第 1-2 周 | EditActivity/画布/参数化管线/撤销重做/基础调节/保存对话框/另存导出；打通入口①② |
| M2 AI 核心 | 第 3-4 周 | LiteRT 推理引擎接入、模型交付管线（内置/按需下载/SHA-256 校验/版本管理/降级链）、AI 自动增强、前后对比、处理中/失败态全套反馈 |
| M3 滤镜与打磨 | 第 5 周 | 内置滤镜 + 强度、滤镜缩略图性能、黑白 UI 走查、PressEffect/转场对齐 |
| M4 P1 功能集 | 第 6-7 周 | 人像/风光/降噪/细节重建/清晰度/色彩增强/一键修复（含大模型分块推理与全分辨率精细渲染流程）、覆盖保存、自定义预设、通知直达 |
| M5 验收与发版 | 第 8 周 | 全量验收清单、基准机+低端机+华为无 GMS 机性能测试、盲测调优、合入主干 |

P2 功能（批量编辑、会话恢复、HEIC、RAW 评估）进入下一迭代池，不占本次排期。

---

## 11. 待确认问题与风险

### 11.1 待确认问题

| # | 问题 | 影响 | 建议决策方 |
|---|------|------|-----------|
| Q1（已定） | 模型来源已定为方案 D 开源组合（NAFNet-w64 / Real-ESRGAN_x4plus / SCRFD-10M / BiSeNet / RetinexFormer / EfficientNet-Lite2 / Image-Adaptive LUT），推理框架 LiteRT；**遗留法务审查项**：InsightFace SCRFD 权重的商用限制、Real-ESRGAN BSD-3 免责条款、NAFNet/BiSeNet 仓库许可 | 法务风险 | 法务审查（发版前置门槛） |
| Q2 | 「人像优化」是否包含磨皮美白之外的塑形（瘦脸等）？当前 PRD 定位为「画质向优化」而非美颜相机 | 产品定位 | 产品 |
| Q3 | NEF/RAW 的处理策略：本期入口完全隐藏，还是置灰并引导「先转 JPEG（依赖相机内 JPEG）」？ | 用户预期管理 | 产品 |
| Q4 | 云端增强是否立项？若立项，隐私授权页、数据留存策略、服务商选型均需前置 | 架构与合规 | 产品 + 法务 |
| Q5（已定） | 分发策略已定为拆分交付：内置模型 ~15MB 随 APK（一键增强开箱即用），高级模型包 ~100MB 按需下载；完全离线场景下降级为传统算法 | 分发策略 | 已决策 |
| Q6 | 导出目录 `DCIM/NikonLink/Edited/` 是否会被「本地照片」查询（`RELATIVE_PATH LIKE '%NikonLink%'`）命中已验证可行，但备份目录 `.backup` 需确认不被 MediaStore 索引展示 | 体验细节 | 开发验证 |

### 11.2 风险与缓解

| 风险 | 等级 | 缓解措施 |
|------|------|---------|
| 端侧模型效果不及预期，弱于 Snapseed/Lightroom | 高 | M2 结束设盲测关卡，不达标则换模型方案或收缩为「滤镜+基础调节」先行发版，AI 增强延后 |
| 低端机 GPU Delegate 兼容性碎片化 | 高 | 推理后端三级回退：GPU → NNAPI → CPU（多线程）；建立测试机矩阵（含麒麟/联发科） |
| 45MP 大图 OOM | 中 | 强制预览降采样 + 导出分块处理 + 内存预算监控（§8.4） |
| APK 体积膨胀影响分发转化 | 中 | 已按 §8.5 拆分：APK 仅增 ~15MB，高级模型包按需下载 |
| 高级模型包下载转化损失（用户拒绝下载 100MB） | 中 | 内置功能开箱即用，下载提示仅在使用对应功能时出现；下载失败自动降级传统算法，体验不中断 |
| 大模型全分辨率处理耗时超出用户耐心 | 高 | 预览实时 + 精细渲染双轨制；显式进度 + 可转后台 + 可取消；「快速导出（长边 4K）」替代路径 |
| SCRFD / BiSeNet 许可不通过 | 中 | 法务前置审查；不通过则降级 YuNet-S（Apache-2.0）+ 轻量皮肤分割模型 |
| 分块推理出现接缝或块间色调不一致 | 低 | 16px 重叠边距 + 块边界羽化后处理；验收纳入 45MP 大图走查 |
| AI 推理耗时超性能指标 | 中 | 导出时允许「快速导出（长边 4K）」选项兜底；推理与写盘流水并行 |
| 覆盖保存误操作引发用户数据丢失投诉 | 中 | 强制二次确认 + 隐藏备份目录 + 30 天保留；默认项永远是「另存」 |
| 滤镜/AI 效果与黑白极简 UI 的视觉冲突 | 低 | 效果内容彩色、UI 控件严格灰阶，设计走查纳入 M3 验收 |
| 人脸检测在无 GMS 设备上的可用性 | 低 | 不用 ML Kit，采用与主推理框架一致的端侧人脸模型 |

---

*文档结束*
