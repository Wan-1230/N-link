# N-Link 竞品分析（2026 Q3）

> 版本基准：N-Link v2.3.1（versionCode 23，`app/build.gradle.kts:24-25`）· 分析日期 2026-09-20 · **修订 2026-09-21：影控台从「背景条目」升为独立档案 §2.11（App Store 一手取证 + 用户一手「安卓内测」信息），能力矩阵加列** · **复核 2026-09-24：新增 §7.1 更新动向一手复核（GitHub API + iTunes lookup），ZTransfer 由「速度威胁」降级为「技术线索」，影控台升为本期唯一仍在扩张的商用对手** · **本文的代码基准仍是取证当时的 v2.3.1**；我们侧已发到 **v2.3.2（versionCode 24，2026-09-24）**，其改动对能力矩阵的影响已就地标注（GAP-05 / GAP-11 / GAP-13 / GAP-27 四行）
> 结论去向：本文是 [PRD-N-Link-v2.5-差距收敛与可信性基建.md](PRD-N-Link-v2.5-差距收敛与可信性基建.md) 的输入，
> 需求编号以 PRD 为准，本文只负责「事实 + 判断」，不排期。

---

## 0. 取证方法与证据等级

本文的每条结论都标注证据等级。竞品多为闭源、私域分发，**不看证据就写对比 = 编故事**，所以先把口径立清楚。

| 标记 | 含义 | 来源 |
|------|------|------|
| 【实】 | 代码/产物直接确证 | 本仓库源码 `文件:行号`；或竞品 APK 静态分析产物 |
| 【官】 | 厂商官方页面/说明书原文 | nikon.com.cn / nikon.co.uk / 在线说明书 / App Store 商店页 |
| 【用】 | **使用者一手信息**（本人在内测群/真机上见到，公开渠道查不到） | 用户口述，本文按「已发生但未公开可查」处理 |
| 【网】 | 公开网络二手信息，**可信度待核** | 第三方综述、开发者自宣视频 |
| 【推】 | 由上述证据推导的判断，非事实 | 本文作者 |

**取证手段**（竞品闭源，只能黑盒 + 结构分析）：

- `aapt2` Manifest/badging 解析 → 权限、SDK 版本、包名、组件：`_apk_re/badging/*.txt`
- DEX 类清单与字符串扫描 → 协议常量、私有命令码、日志文案（**日志文案是竞品把自己的设计写给自己看的东西，信息密度最高**）：`_apk_re/dex/`、`_apk_re/scan/*.txt`
- 原生库导出符号（含未剥离的 C++ mangled name）→ 还原 SDK 类结构：`_apk_re/so/`
- 反汇编取证主机注册流程：`_apk_re/zdrop_hostreg_disasm.txt`
- 法律边界：**全部为公开分发物的结构与字符串分析，不复制任何竞品代码**；`libcamerawirelesscontrol.so`（像素蛋糕内第三方商业 SDK）仅用于理解行业做法。

**不可得清单（诚实记录）**：ZDROP、ZRelay、影犀三款在公开网络**没有可查的商店页/官网/定价页**（Google Play、国内容器、知乎、B 站、小红书均无有效条目，ZRelay 仅有开发者自宣视频）。它们的版本号、能力面、私有命令**只能靠 APK 取证补齐**。这本身是一个结论：**这条赛道没有公开可比的商业情报，谁先做出「可被搜索到的、有真机数据的」产品，谁就拿到信任红利。**

> **例外（2026-09-21 修正）**：影控台**有**公开商店页、定价、评分与逐版 changelog（§2.11），上面那句「没有公开可比的情报」对它（和对像素蛋糕）不成立。反过来说更值得警惕：**它是这条赛道里把自己做成「可被搜索到」的那一类** —— 而我们还只活在 GitHub 和网盘链接里。

---

## 1. 赛道地图：三层玩家，两种动机

```
                        连接/传图/遥控/监看 —— 尼康 Z 系列手机侧
                                     │
      ┌──────────────────────────────┼──────────────────────────────┐
      │                              │                              │
  ① 官方闭环                    ② 国内商业/私域                  ③ 开源同路人
  Nikon 自己，卖相机的人        独立开发者/小团队，靠口碑+私信     MIT/无 License，靠 GitHub
  ─ SnapBridge 2.13.3【官】     ─ ZDROP 1.0.257【实】            ─ 帧澈 ZENCHE v1.5.15【实】
  ─ 影速传 NX MobileAir 1.6+【官】 ─ ZRelay 3.0.46【实】            ─ ZTransfer v1.0.2【实·外部】
  ─ 赛事通 / NX Tether【官】    ─ 影犀 1.2.1【实】                ─ aero-shutter v0.8.8【实·外部】
  ─ WMU（实际已废弃）【官】      ─ 像素蛋糕 1.9.2【实+官】           ─ libgphoto2 / darktable（桌面）
                                ─ 影控台 2.2.0【官】（iOS 已上架；Android 内测【用】）
                                ─ Zensō/传Z（iOS 为主）【网】
                                     │
                        ★ N-Link v2.3.1：横跨 ②③ 的独一份定位
                          （开源 + Android + 全链路 + 尼康 BLE 配对 + 商业级连接诊断）
```

**判断【推】**：这不是一个「我们 vs 官方」的赛道，而是**官方能力缺口的填空竞赛**。尼康官方 App 的每一个硬限制（2MP、无 USB、后台不传、云端服务关停）都是第三方存在的理由；反过来，官方每一次「免费化」（影速传 1.6.0）都会直接压缩第三方的卖点空间（见 §5）。

---

## 2. 逐家档案

### 2.1 SnapBridge（尼康官方）— 所有人的反面教材

| 项 | 内容 | 证据 |
|---|---|---|
| 包名/版本 | `com.nikon.snapbridge.cmru` 2.13.3；Android 国内包停在 2.12.0 | 【实】`_apk_re/badging/SnapBridge_v2133.txt`【官】商店页 |
| 通道 | BLE + 蓝牙经典(SPP) + WiFi AP/STA；**无 USB host**（不声明 `android.hardware.usb.host`） | 【实】Manifest |
| 技术栈 | Java/Kotlin + 3 个尼康自研原生库：`libNis.so`（NIS 云）、`libJuno.so`（EXIF）、`libLsSec-jni.so`（BLE 凭据加密） | 【实】`_apk_re/so/` |
| 配准 | Bonjour `_ptp._tcp.` + GUID 配对；用 PTP **厂商操作读写相机 WMA 设置**（SSID/WPA 密码/信道/认证/WPS） | 【实】类名 `GetWmaSettingAction`/`SetWmaSettingAction`/`BonjourPairingTask` |
| 能力边界 | 自动传图仅 **2MP web-ready**；大尺寸 JPEG/视频需手动；**后台运行时不自动上传**、位置仅前台下载 | 【官】[Nikon UK 产品页](https://www.nikon.co.uk/en_GB/product/apps-software/snapbridge)、[Z8 说明书](https://onlinemanual.nikonimglib.com/z8/zh-cn-prc/problems_and_solutions_371.html) |
| 更新动向 | 一年 3–5 版，内容基本是「新增机型支持 + 修小缺陷」；2.13.3 发布于 2026-01-06 | 【官】App Store 版本史 |
| 口碑 | 中国区 iOS 4.7/18428 评（商店口径），**但第三方聚合口径 1.9/5、1 星占 64%**；中文社区高频词「乐色/玄学连接/配对失败/只给小图/NEF 传不了/后台一杀就断」 | 【网】+【实】我们自己的群反馈 |
| **生态地震** | **Nikon IMAGE SPACE / 影像馆 2026-10-15 终止服务**，接棒的 Nikon Imaging Cloud **仅 Z6III/Z5II/Zf/Z50II**；2024-04 尼康中国曾一次性下架全部安卓官方 App（备案问题） | 【官】[尼康中国 Apps 页](https://www.nikon.com.cn/sc_CN/service/app.page)、[IT之家](https://www.ithome.com/0/994/605.htm) |

**对我们的三条直接冲击**【推】：
1. 官方永远不做 USB 有线、不做原图批量、不做低延迟监看 —— 这是我们的**结构性护城河**，不是功能优势。
2. 影像馆关停会把一批「云端备份」用户推到本地/网盘工作流，是**传原图 + 不上传云**叙事的窗口期。
3. 官方 App 有合规停摆先例 → 我们「纯本地、无云端依赖、开源可审计」可以作为**卖点写进对外文案**。

### 2.2 影速传 NX MobileAir（尼康官方专业传图）— 2026 年最被低估的威胁

- 【官】官方页原文：**「自 1.6.0 版本起 Android 专业版将解除对相册和保存文件的功能限制，现可免费下载使用。」**
- 【官】能力：**USB 有线联机**取图 + 经 4G/5G 向 FTP 服务器推送；Z9/Z8/Z6III 支持 Frame.io 上传；iOS 无线传输与遥控连拍限 Z9/Z6III 且需 iOS 17。
- 【推】**这是官方第一次把「有线联机 + 原图」免费化**，正面打在我们「USB 读卡 + 传原图」的核心卖点上。它的弱点同样明显：面向商业工作流（棚拍/体育/图片直播），**没有实时监看工具、没有遥控拍摄参数面、没有 B 门/间隔、没有 LUT/波形、不做 BLE 保活**，且强依赖云端上传链路。
- **对策**【推】：不要在「官方已经免费做的部分」上纠缠，把叙事挪到官方做不到的三件事：**机身参数全量遥控 + 低延迟监看工具 + 一次配对长期自动恢复的稳定性**。

### 2.3 ZDROP（`com.zdrop.z6ii` 1.0.257）— 无蓝牙兜底的极端工程派

| 项 | 内容 | 证据 |
|---|---|---|
| 体量/栈 | **3.1MB**，纯 Kotlin + Compose，单 dex，**零第三方网络库**（无 OkHttp/Retrofit） | 【实】`_apk_re/badging/ZDROP_1.0.257.txt`、`dex/classes_ZDROP-1.0.257.txt` |
| 通道 | AP + 手机热点 STA + 同路由 STA + USB host；**完全不声明蓝牙权限** | 【实】Manifest（无 `BLUETOOTH_*`） |
| 关键手法① | **不硬编码 IP：从已连网络的 DHCP/路由信息学出相机地址**（网关即相机），并做「稳定化」与「排除手机自身地址」 | 【实】字符串 `AP gateway detected host=` / `AP gateway stabilized host=` / `AP gateway ignored because it matches handset address=` |
| 关键手法② | NSD 发现 + resolve 队列 + `/24` 扫描 + **仅 TCP 可达性探测**（不抢会话）；热点模式额外等相机拿租约（`AP network readiness wait=1200ms`） | 【实】`PtpIpProbe` 类与字符串 |
| 关键手法③ | **双 data 通道**、断点续传、按阶段(phase)打点重试 | 【实】`… retry requested phase=`、`channel retry scheduled failedAttempt=` |
| 相机热点识别 | SSID 规则 `(?i)^NIKON_([0-9A-F]{8})` | 【实】DEX 字符串 |
| 主机注册 | `0x952B`(PrepareHost) / `0x935A`(ConfirmHost) 流程的**原始出处**（我们的实现来自其反汇编） | 【实】`zdrop_hostreg_disasm.txt` |
| 商业/分发 | 【网】公开网络查无商店页与定价 —— 私域分发 | — |

**启示**：AP 通道跨机型不稳的第一根因是「会不会学网关」，而不是 ROM 差异。我们已照此落地 `ApGatewayResolver.kt`（v2.2 G1，已验证）。**它没有 BLE，所以每次连接都必须把网络侧做对** —— 这解释了它把工程量全压在发现/探测上。

### 2.4 ZRelay 相机快传（`com.geekmanlab.zrelay` 3.0.46）— BLE 主导 + 兼容治理最重投入者

| 项 | 内容 | 证据 |
|---|---|---|
| 体量/栈 | 3.2MB，Kotlin + 极小 native（`libzrelay_core.so` 仅 `JNI_OnLoad`，PTP 栈在 native 侧 C++） | 【实】badging、`dex/classes_ZRELAY.txt` |
| 通道 | **BLE 配对 + WiFi**；**无 USB host**；**不声明 `CHANGE_WIFI_MULTICAST_STATE`**（→ 不做 mDNS 发现） | 【实】Manifest |
| 关键手法① | **`TetheringManager` 主动开/感知本机热点**（`TetheringEventCallback`/`TetheringInterface`），再用 BLE 把热点 SSID/密码写给相机 → 用户不用手工开热点 | 【实】`Landroid/net/TetheringManager$TetheringEventCallback;` |
| 关键手法② | 重连状态机：BLE 扫描窗口 + 候选打分 + **拒绝原因可见**（`reconnect_scan_window_elapsed`、`reconnect_candidate_matched/rejected`、`ReconnectingAfterBond`） | 【实】`scan/ZRELAY.txt` |
| 关键手法③ | **兼容规则放服务端下发**（`/v1/camera-rules`） | 【实】API 路径字符串 |
| 合规 | **targetSdk 36（Android 16）**，比我们高一档 | 【实】badging |
| 商业/定价 | 【网】免费 + 约 ¥20 一次性 Pro（非订阅），RAW/JPEG 下载与断点续传，v3.0 加光圈/ISO/WB/快门/实时取景；**经 B 站私信与官网分发，无商店上架** → 综述明确指出「带来隐私与持续维护疑虑」 | 【网】[smzdm 综述](https://post.smzdm.com/p/a958wn95/)（**AI 聚合稿风险，价格与版本需以 APK 复核**） |

**启示**：①「机型×ROM×通道」矩阵必须当产品资产建 —— 它是同类里唯一在这一点上重投入的，且用服务端下发做到不发版修兼容；我们已把规则数据化为 `assets/compat/rom_rules.json`（6 家族/19 条），**但没有热更新通道**（G21 未做）。②`TetheringManager` 是我们唯一的结构性空白（G4 全仓 0 命中），能把 STA 从「用户手工开热点 + 相机手工连」压成一步。③targetSdk 36 是必须跟进的合规项。

### 2.5 影犀 / CameraSync Pro（`com.camerasyncpro.app` 1.2.1）— Flutter 多品牌路线

- 【实】Flutter（Dart 逻辑在 `libapp.so`）+ 原生 USB 插件；73MB；minSdk 26 → **targetSdk 36**；USB + PTP/IP，多品牌（尼康/佳能/富士/索尼/松下/GoPro/佳能 BJNP）。
- 【实】**九种 mDNS 服务类型枚举**：`_ptp._tcp`、`_nikon._tcp`、`_nikon-ptp`、`_nikon-pc`、`_camera-canon._tcp`、`_fujifilm._tcp`、`_sony._tcp`、`_panasonic._tcp`、`_gopro-web._tcp`… （**我们只监听 2 种**：`WifiScanner.kt:88-89`）。
- 【实】STA 侧三个子状态：`nikonStaInitAckOnly` / `nikonStaPairingConfirmed` / `NikonStaConnectionIntent` —— 把「STA 连不上」拆成可诊断的子阶段，与我们 `ConnFunnel` 的思路一致，**但它的 STA 子状态比我们的 Stage 更贴近尼康语义**。
- 【实】内置 **40 个 33³ `.cube` LUT**（Color Grading Central / Denver Riddle 版权，**不可照抄**）+ 10 个水印模板 + exiftool；**无自定义 GLSL** → 推断 LUT 用于后期导出而非实时监看【推】。
- 【网】公开网络仅 2026-08-25 一篇综述提了一行「刚起步，观望，支持尼康佳能」。

**启示**：多品牌是它的广度优势，也是它的深度天花板 —— 每个品牌只能做到 PTP 公共子集。我们做单品牌深耕，**必须把尼康私有语义吃干**（我们已做到：拍摄后逐张 `ObjectAddedInSDRAM` 入册、`LiveViewProhibitCondition` 禁读原因、0x9207→0x100E→0x100F 三级快门降级）。mDNS 类型枚举数与 STA 子状态数是**低成本可抄的两件事**。

### 2.6 像素蛋糕 PixCake（`com.xiangtian.pixcake` 1.9.2）— 花钱买 SDK + 订阅制

- 【实】内置第三方商业 SDK `libcamerawirelesscontrol.so`（**111 个 `NikonCameraControl::` 符号**）→ 走「授权/购买 SDK」路线，自研成本最低，能力面受限于 SDK。
- 【官】小米商店 Android v1.9.2（2026-09-14）；**订阅 ¥19.9/月 或 ¥198/年**；主业 AI 修图，「联机拍摄」是前端入口；官方 wiki 列索尼/佳能/尼康/富士/理光。
- 【推】**它证明了两件事**：这条链路有人愿意付订阅费；「AI 修图 + 联机」是可以互相导流的组合（我们 Phase 4 的 AI 修图 PRD 与此同构，但我们不能买 SDK、只能自研协议，因此工程门槛远高于它）。

### 2.7 帧澈 ZENCHE（`Tauber01/ZENCHE`，MIT，125★，v1.5.15）— **威胁最大的同路人**

> 用户点名的「帧澈」是**开源项目**，不是 APK。这是本次分析里唯一能读到全部源码的对手。

- 【实】五端原生：macOS(SwiftUI + libgphoto2)、**Android(Views + USB Host，语言是 Java 1.25MB)**、Windows(WPF/.NET8 + libusb)、HarmonyOS(ArkUI)、iOS。唯一贡献者 62 commits，仓库创建 2026-07-28，**最后 push 2026-08-22（09-24 复核已 33 天无提交，★125→126）**；27 个 release，一个月内平均 1.6 天一版。
- 【实】协议栈：TCP 15740 双通道（command + event），**Live View 用尼康私有 op `0x9201` 开 / `0x9202` 关 / `0x9203` 取帧（data-in 返回 JPEG）**，录像 `0x920a/0x920b`；取帧循环**约 10fps**，单帧失败 300ms 退避；外录为流式 Motion-JPEG AVI。
- 【实】能力面：50 款机型档案（20 Nikon 含 Z9/Z8/Zf/Z6III/Z50II/Z5II/ZR，16 Sony + 14 Canon **均标注「实验性，待实机验证」**）；`.cube` LUT（`CubeLut.java`）、**RGB 三色叠加波形 + 矢量示波器**、直方图、峰值对焦、假色、斑马纹、**107 款尼康云创 NP3 预设 SDR 近似监看**；间隔/包围/焦点包围/B 门；机内存储浏览 + 批量下载 + 删除；FTP/HTTP/WebDAV 收件箱；XMP GPS 旁车；RAW+JPEG 配对；**AI 修图/生图**。
- 【实】**它的「开源」只有客户端**：AI 是 `zenche.top` 闭源云服务 + 设备绑定激活码 + 爱发电付费兑换码。桌面端**接入专有 Nikon Remote SDK 2.0.0 + Image SDK 1.46.0 二进制**（不入库，README 自述「官方运行库属于尼康专有组件」）。
- 【实/推】死穴：①**BLE 不是相机配对** —— 只有 `BluetoothRemoteController`（自家蓝牙遥控快门），全仓搜 `0x952B`/`_ptp._tcp`/`SnapBridge`/`blowfish` **0 命中**，即它**没有尼康 BLE 配对协议实现**；②无 RAW 解码，NEF 预览靠系统；③Android 包是 **debug 证书签名**、iOS 未签名、Windows 无 Authenticode；④开放 issue #44「Win11 安装后打不开」，官网更新通道仍停在 1.5.14；⑤无线收件箱默认口令 `nikonlink:nikonlink` 且无 TLS；⑥Sony 实时取景/录像标注「不支持」；⑦自 8/22 起停更。
- ⚠【实】**命名冲突**：`github.com/Tauber01/NikonLink` **永久重定向到 ZENCHE**，其 v0.4.0–v0.8.1 的 release 标题就是「Nikon Link」（2026-07-28/29），README 明说目录/scheme/**包名 `com.tauber.nikonlink` 永久保留**。我们首提交 2026-08-02（`Initial commit: NikonLink Android app`），`applicationId = com.nikonlink.app` —— **对方早 5 天，且握有 GitHub 搜索与外链权重**（本文 §6.3 GAP-19；v2.5 PRD **没有**为它立 FR，已作为待拍板项列在 PRD §10.2 第 7 条——旧版本文写的「详见 PRD FR-20 品牌决策」指向一个不存在的编号，2026-09-24 订正）。

**威胁判断【推】**：帧澈构成的是**叙事威胁**而非工程威胁 —— 它的「五端 + LUT + 波形 + AI + 批量 RAW + 文件库/备份/哈希」在 README 层面完胜我们的功能表，但它没有尼康 BLE 配对、Android 端是 Java/debug 签名、且已停更 33 天。我们的应对不是追功能清单，而是**把「连接成功率 + 真机验证 + 每周发版」这三件它能说但我们能证明的事做穿**，同时把它已验证的需求方向（LUT/波形/包围/焦点包围/RAW 配对）作为我们的 P1 输入。

### 2.8 ZTransfer（`RealCaCl2/ZTransfer`，v1.0.2，2026-08-21，★6，**无 License**）— 同平台同细分的后起者

- 【实·外部】Nikon Z 专款联机拍摄，Android only，Flutter(Dart) + **Kotlin 289KB**；USB PTP/MTP + PTP/IP；mDNS `_ptp._tcp` + `_nikon-ptp._tcp` 发现；`NetworkRouteSelector`/`LocalAddressSelector`。
- 【实·外部】**私有配对码与我们的实现不一致**：`PtpIpConstants.kt:52-63` 声明 GetPairingCode **`0x9838`**、ConfirmPairing **`0x93D4`**、ChangeApplicationMode `0x9280`、GetEventEx **`0x9714`**、GetPartialObjectEx `0x9918`、**`GetPartialObjectHighSpeed 0x9A6C`**、AdvancedTransfer `0x907C`，并注明「取自 `libztransfer_core.so` 偏移 `0xdfe0` 的 nativePrivateOpcode 查找表（55 项 × 8 字节），是已验证的查表值而非猜测」。原文逐字：
  > `// Nikon "Connect to computer" mode — opcodes provided by libztransfer_core.so`
  > `// The previous hardcoded values (0x9010, 0x9435, 0x935A, 0x952B, 0x9431, 0x9400) were ALL wrong and caused the camera to reject every private operation.`
- 【推】**这是本次分析里最需要立刻处理的技术事项，但结论要读准**：① 对方明确把这套码限定在 **Nikon「Connect to computer」模式**，而我们的 `0x952B/0x935A` 服务的是 **SnapBridge 系的 WMA 主机注册**（`zdrop_hostreg_disasm.txt` 取证，且真机跑通）→ **两条通路并存的可能性远大于「谁错了」**，不能因为这句话就把已验证的代码判死。② 真正需要立刻核对的是 **GetEventEx**：我们代码里只有 `OP_NIKON_CHECK_EVENT = 0x90C7`（`PtpProtocol.kt:96`），**全仓不存在 `0x9714`**；而 `PtpSessionManager.kt:321-322` 的注释把这个调用写成「Nikon GetEventEx (0x90C7)」——**命名与注释自相矛盾**（0x90C7 按标准命名是 CheckEvent，只问「有没有事件」，不取事件数据）。若 `0x9714` 才是带事务号回取事件体的 GetEventEx，**我们可能一直在用「只查不取」的调用**，这与 GAP-05「数据期丢事件」是同一根链上的两个症状。③ `0x9A6C` 高速局部对象下载若成立，是全场唯一一次下载速率跃升的机会。以上全部列入 PRD FR-14 探测专项。
- 死穴：无取景、无遥控参数、无监看、无 BLE、★6、**无 License（默认保留所有权利，不可引用其代码）**。
- ⚠ **2026-09-24 复核下调**：GitHub API 显示其 push 仍停在 2026-08-21（34 天无提交、0 open issue），**并非此前判断的「增速最快的同细分对手」**。它的价值改为**技术线索来源**：那份从 native 表提取的私有 op 清单（`0x9838/0x93D4/0x9714/0x9A6C/0x9918`）值得我们在 GAP-25 / PRD FR-15 里**自行探测复核**，但因其无 License，**不得引用其代码或数值作为实现依据**。

### 2.9 aero-shutter（`subhashraveendran/aero-shutter`，MIT，v0.8.8，2026-07-24）— 传输工程参考

- 【实·外部】纯 PTP/IP，Go 核心 + Capacitor 壳（iOS/Android）+ 桌面 TUI；**4MiB 分块断点续传 + 放大 TCP 窗口 + TCP_NODELAY + 平坦内存**；**复用尼康官方 App 的固定 initiator GUID/友好名，让相机免重复配对**；网关/DHCP 并发探测零配置。
- 【实·外部】issue #1：**导约 5 张即死锁（在 PTP 互斥锁内发进度）** —— 与我们 v2.2 修掉的「缩略图与原图互相插队」同型缺陷，说明**这是 PTP/IP 单会话串流的天坑**。
- 【推】它的「伪造官方 GUID 蹭已配对状态」是一条我们**不该走**的路（合规与用户信任风险），但「4MiB 分块 + TCP 窗口调优」可以直接对照我们的 4MB 分块做速率基线实验。

### 2.10 iOS/其他商业同类（信息不完整，仅作背景）

- 【网】Zensō/远映（iOS，6 天试用 + ¥68 买断）、Z传（免费 APK）、ZLinkLab（鸿蒙，¥9.9 早鸟）。
- ⚠【推】上述均来自疑似 AI 聚合稿（曾把某项目写成 "NikonLink"、条目价格互相矛盾），**只能当线索**。
- 注：**影控台已从本节移出**，独立成 §2.11 —— 它的能力面与威胁等级不在「背景」这一档。

### 2.11 影控台 / Imaging Console（iOS `id6768196167`，¥148，v2.2.0）— **能力面最完整的一方，且正在进 Android**

> 取证方式与其他家不同：这是**唯一一款在公开商店可查、带评分与完整版本历史**的对手，所以本节以 App Store 中国商店页为准（2026-09-21 取），而非 APK 逆向 —— 它没有 APK 可逆向（Android 尚未公开分发）。

| 项 | 内容 | 证据 |
|---|---|---|
| 身份 | 「影控台」（Imaging Console），开发者 **栋 张**，类别 摄影与录像，年龄 4+；姊妹品「影控台 Lite」（`id6788416417`） | 【官】App Store 中国页 |
| 体量/门槛 | 157.7MB；**iOS 17.0 起**；iPad 与 macOS 窗口适配（changelog 多次提到 macOS 自定义保存文件夹、iPad 窗口大小） | 【官】 |
| 口碑 | 主版 **4.6/5（55 评）**，Lite **4.1/5（7 评）** | 【官】2026-09-21 |
| **平台** | **iOS 已上架；Android 正在内测（用户一手信息，公开渠道查不到任何安卓包）** | 【用】 |
| 通道 | **USB（MTP/PTP）+ 无线 PTP/IP**，无线含「相机热点 / Wi-Fi / 移动热点」三种；描述**未提蓝牙配对** | 【官】 |
| **机型分层** | **Expeed 7 完全兼容**：Z9 / Z8 / ZR / Z6III / Z5II / Zf / Z50II；**Expeed 6 部分功能受限**：Z5 / Z6 / Z7 / Z6II / Z7II / Z50 / Z30 / Zfc，且老机型经「连接至计算机」方式**无法访问机内文件** | 【官】 |
| 监看工具 | 触控对焦、**LUT 预览、伪色、斑马纹（可调数值/范围）、直方图、波形图、安全区、水平仪、网格**、变形宽银幕反挤压、码率显示、纯净显示、LUT 烘焙 | 【官】 |
| 多机 | **1:N 无线群组**：同步触发照片/视频、实时监看与特殊模式同步 | 【官】 |
| 文件工作流 | 浏览/回放/下载/**保护**/**评级**/LUT 预览、批量下载、按日期全选、筛选、已下载标识、旗舰机型语音备忘录 | 【官】 |
| **「速传」工作流** | 自动下载新文件；**在相机端把照片标记为保护 → App 只传入被标记的那些**；存至图库/照片/文件；附加 GPS/IPTC/版权模板；**无卡拍摄** | 【官】v2.0.0 引入 |
| 生态接线 | **巧摄（Planit）曝光指南**（需 CSV）、NP3 优化校准导入、内置 VM-Natural、水印（角落/摄影卡片/自定义图标）、LivePhoto 导出（保留 HDR + 附加 GPS）、PZ 电动变焦控制与位置预设、灵动岛/实时活动 | 【官】 |
| 商业模式 | **三层漏斗**：Lite 免费（AF-ON 遥控、曝光、iMenu、手动下载）→ **Lite Plus 订阅 ¥9.9/月 或 ¥68/年**（速传、实时监看、LUT、连拍控制、自定义图库筛选）→ **主版 ¥148 买断**（多机集群、全局同步、斑马/直方图等专业监看、高级自动化） | 【官】 |
| 节奏 | 1.0 首发 **2026-07-02** → 2.2.0 **2026-09-10**：**10 周、商店页可数 21 个版本条目 ≈ 3 天一版**，且每版 changelog 写到功能粒度（含「速传」这样的整条工作流） | 【官】版本历史逐条可数 |

**启示（这一节的信息量比前面所有家加起来都大）**：

1. **【推】「Android 平台独占」这个前提正在失效**。此前我们对 iOS 的判断是「iOS 侧无法直连尼康、只能靠官方」，影控台把这条推翻得更彻底：iOS 不但能连，还能做出 **1:N 集群 + 伪彩 + 波形 + 速传** 这种完整度。它进 Android 内测意味着**我们所在的唯一平台将在几个月内出现一个能力面完整的付费对手**，而它带来的不是功能竞争，是**「尼康 App 应该收费」这个心智竞争**。
2. **它的功能重心精确压在我们 §5.3 的两条劣势上**：监看工具（GAP-11 我们全空白，它从伪彩到波形到安全区到反挤压一整套，且明确放进买断档当付费理由）、RAW+JPEG 配对（其 changelog 里 1.1.0/1.2.0/1.3.1 反复处理 RAW+JPEG 合并显示与下载选项 → 说明这是真实高频痛点，GAP-13）。
   同时把边界说准，避免被上一版那份 AI 聚合稿带偏：**峰值对焦（peaking）与包围/焦点包围在它的商店描述里一次都没出现** —— 旧版本文写它「带示波器/直方图/LUT/峰值对焦」中的「峰值」是聚合稿的错，已订正。这两项目前仍只有帧澈宣称做过，是我们差距里唯一还没被商业对手证明「值得做」的部分。
3. **「相机端标记保护 → 只传被标记的」是我们没有、且几乎零协议成本的能力**。我们【实】`TransferManager.kt:839` 已经在解析 ObjectInfo 的保护位，但全仓 **没有 `0x101A SetObjectProtectionStatus`**（`PtpProtocol.kt:48-65` 的操作码表到 `0x101B` 为止）→ 读得到、写不了。尼康机内按一次「保护」就能把「我要传哪几张」说清楚，**不需要私有命令、不需要云端、不需要后台常驻**，是这条赛道里性价比最高的工作流发明。→ 新增 **GAP-27**。
4. **发版速度从「差异点」降为「入场券」**（§7 已改）。我们 22 个 tag / 36 天 ≈ 1.6 天一版，它 22 版 / 10 周 ≈ 3 天一版 —— 我们仍然更快，但**同一档**。差别在兑换方式：它的节奏变成商店页上的评分与逐版 changelog，我们的节奏变成提交记录和网盘文件，**普通用户看不见也验证不了**。
5. **变现结构不该由本文建议**（PRD §1.3 已把「订阅/付费墙」列为本期非目标）。但要把事实记下来：这条赛道**已被验证存在两档付费意愿**——¥148 买断（主版，55 评）与 ¥9.9/月（Lite Plus）。对手用它做分层，我们做免费开源，**这在获客上不吃亏，在「谁更值得信任」上占优，在「谁更可持续」上存疑**。

---

## 3. 横向能力矩阵

> ✅ 有 / ◐ 部分 / ✗ 无 / ❓ 不可考 / ❗ **对方通篇描述与 changelog 均未宣称**（与 ❓ 不同：不是查不到，是它没做/没说的可能性更大）。
> **「私有命令」列是关键**：只有做私有扩展才能拿到尼康的真实体验（BLE 配对、原图高速、逐张事件、监看）。
> 影控台列全部取自 App Store 商店页与版本历史（2026-09-21），闭源无 APK 可逆向 → 标【官】而非【实】。

| 能力 | SnapBridge | 影速传 | ZDROP | ZRelay | 影犀 | 像素蛋糕 | 帧澈 | ZTransfer | 影控台 | **N-Link** |
|---|---|---|---|---|---|---|---|---|---|---|
| 平台 | And/iOS | And/iOS | **And** | **And** | **And** | And/iOS/桌 | 五端 | And | **iOS/iPad/Mac**（And 内测【用】） | **And** |
| 开源 | ✗ | ✗ | ✗ | ✗ | ✗ | ✗ | ✅ MIT | ✗(无证) | ✗ | ✅ 无 LICENSE⚠ |
| **BLE 尼康配对** | ✅ | ✗ | ✗ | ✅ | ◐ | ❓ | ✗ | ✗ | ✗（描述未提蓝牙） | **✅【实】** |
| WiFi AP 直连 | ✅ | ✗ | ✅ | ✅ | ✅ | ✅ | ✅ | ◐ | ✅【官】 | **✅【实】** |
| WiFi STA（相机连热点） | ✅ | ✗ | ✅ | ✅ | ✅ | ❓ | ✗ | ◐ | ✅【官】 | **✅【实】** |
| **USB 有线** | **✗** | ✅ | ✅ | **✗** | ✅ | ❓ | ✅ | ✅ | ✅【官】 | **✅【实】** |
| 网关/DHCP 学地址 | ✗ | ❓ | ✅ | ◐ | ✗ | ❓ | ✅ | ✅ | ❓ | **✅ v2.2【实】** |
| mDNS 类型数 | 1 | ❓ | 少 | 0 | **9** | ❓ | 2 | 2 | ❓ 闭源不可考 | **2【实 WifiScanner.kt:88】** |
| 主动开本机热点 | ✗ | ❓ | ✗ | **✅** | ✗ | ❓ | ✗ | ✗ | ❓ | **✗（结构性空白）** |
| 相册浏览/分页 | ◐ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ 日期分段/筛选【官】 | **✅ 18/页【实】** |
| 元数据批量拉取 | ❓ | ❓ | ✅ | ❓ | ❓ | ❓ | ❓ | ❓ | ❓ | **✅ 0x9805【实】** |
| 断点续传 | ✗ | ❓ | ✅ | ✅ | ❓ | ❓ | ✅ | ✅(4MiB) | ❓ | **✅ 101B【实】** |
| >2GB 大文件 | ✗ | ✅ | ❓ | ❓ | ❓ | ❓ | ❓ | ❓ | ❓ | **◐ 仅安全化【实 :747】** |
| 高速局部对象 op | ❓ | ❓ | ❓ | ❓ | ❓ | ❓ | ✗ | **✅ 0x9A6C** | ❓ | **✗ 未探测** |
| 并发下载 | ❓ | ❓ | ◐ | ❓ | ❓ | ❓ | ❓ | ❓ | ❓ | **✗ 串行=1【实 :1232】** |
| RAW(NEF) 原图 | ✗ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅【官】 | **✅【实】** |
| **RAW+JPEG 配对策略** | ✗ | ◐ | ◐ | ◐ | ◐ | ◐ | **✅** | ◐ | **✅ 合并显示+下载选项【官】** | **✗【实 未找到】** |
| 删除相机侧文件 | ✗ | ❓ | ❓ | ❓ | ✅ | ❓ | ✅ | ❓ | ❓ | **✅ 0x100B【实 `PtpProtocol.kt:58`+`TransferManager.kt:813`】** |
| 遥控快门/参数 | ◐ | ✗ | ✗ | ✅ | ✅ | ✅ | ✅ | ✗ | ✅ + iMenu【官】 | **✅【实】** |
| B 门 / 定时 / 间隔 | ✗ | ✗ | ✗ | ◐ | ✅ | ◐ | ✅ | ✗ | ❓ | **✅【实】** |
| **包围 / 焦点包围** | ✗ | ✗ | ✗ | ✗ | ❓ | ❓ | **✅** | ✗ | ❗ **描述未提** | **✗【实 0 命中】** |
| 录像遥控 | ◐ | ✗ | ✗ | ❓ | ✅ | ❓ | ✅ | ✗ | ✅【官】 | **✅ 0x920A/B【实】** |
| 拍摄后逐张事件入册 | ✅ | ❓ | ❓ | ❓ | ❓ | ❓ | ◐ | ❓ | ✅「速传」自动下载【官】 | **✅【实 SDRAM】** |
| Live View | ✗ | ✗ | ◐ | ✅ | ✅ | ✅ | **✅ ~10fps** | ✗ | ✅【官】 | **✅ 15fps 固定【实 :39】** |
| 监看帧率自适应 | — | — | ❓ | ❓ | ❓ | ❓ | **✅ 可调** | — | ❗ 未宣称 | **✗ 硬编码 66ms** |
| 硬解/GL 上屏 | ? | ? | ? | ? | ? | ? | ◐ | — | ❓ | **✗ CPU+ImageView【实】** |
| 直方图 | ✗ | ✗ | ✗ | ✗ | ◐ | ✗ | **✅ RGB+波形+矢量** | ✗ | **✅ 直方图+波形【官】** | **◐ 仅亮度 64 柱【实】** |
| LUT / 斑马 / 峰值 | ✗ | ✗ | ✗ | ✗ | **✅(40 cube)** | ❓ | **✅** | ✗ | **✅ LUT/伪色/斑马/安全区【官】** | **✗【实 0 命中】** |
| 触摸对焦 | ✗ | ✗ | ✗ | ❓ | ✅ | ❓ | ✅ | ✗ | ✅ 含定位对焦点【官】 | **✅ 0x9205【实】** |
| 对焦框回显 | — | — | — | — | — | — | ❓ | — | ❗ 未宣称 | **✗ 未标定【实 AfReadback:14】** |
| 连接诊断/原因码 | ✗ | ✗ | ✅ | **✅ 打分+拒绝原因** | ◐ | ❓ | ◐ | ❓ | ❓ | **✅ 12 Stage/40 Reason【实】** |
| 机型×ROM 兼容治理 | ◐ | ◐ | ◐ | **✅ 服务端下发** | ◐ | ✅ | ✗ | ✗ | ✅ **按 Expeed 代际分档【官】** | **◐ 19 条本地/9 条未验证** |
| 兼容规则热更新 | ✗ | ❓ | ✗ | **✅** | ❓ | ❓ | ✗ | ✗ | ❓ | **✗（G21 未做）** |
| 快门次数/EXIF | ◐ | ◐ | ❓ | ❓ | **✅ exiftool** | ❓ | ❓ | ❓ | ◐ IPTC/GPS/版权附加【官】 | **✅ 五层瀑布【实】** |
| 位置/GPS 注入 | ✅(前台) | ✗ | ✗ | ✅ | ❓ | ✗ | ✅(XMP) | ✗ | **✅ 拍摄时附加 + 自定义位置【官】** | **◐ 仅 UUID 声明【实 G24-26】** |
| 本地/网盘更新通道 | ✅ | ✅ | ❓ | ✅ | ❓ | ✅ | ✅ | ❓ | ✅ App Store + 静默检查 | **✅ GitHub+夸克+百度【实】** |
| 崩溃上报 | ✅ | ✅ | ❓ | ❓ | ❓ | ✅ | ❓ | ❓ | ❓ | **✗ 仅本地 crash.log【实】** |
| 英文/多语言 | ✅ | ✅ | ✗ | ✗ | ❓ | ◐ | ✗ | ✗ | ❓ | **✗ 936 处硬编码中文【实】** |
| targetSdk | 35 | ❓ | 35 | **36** | **36** | ❓ | ❓ | ❓ | — (iOS) | **35（落后一档）** |
| 定价 | 免费 | **免费(1.6.0+)** | 不可考 | 【网】免费+¥20 Pro | 不可考 | **¥19.9/月·¥198/年** | AI 云付费 | 无 License | **¥148 买断 / Lite Plus ¥9.9·月【官】** | **免费开源** |

---

## 4. 四条技术路线的取舍（这才是差异的根源）

| 路线 | 代表 | 得到什么 | 付出什么 | 我们的位置 |
|---|---|---|---|---|
| **A. BLE 主导**（配对/凭据/保活全走蓝牙，WiFi 只搬数据） | ZRelay | AP 稳定性最好；可主动开热点；免手工输入密码 | **强依赖机身支持 BLE 配对**；新固件用 LsSec 加密 `0x2004` 凭据后不可解 | 我们已是 BLE 主导派，但**没有 TetheringManager**（G4），BLE 侧只做了配对/心跳，`0x2006` 写时间、`0x2007` 写位置**一个都没实现** |
| **B. 纯 WiFi/USB**（无蓝牙兜底） | ZDROP | 无配对依赖，兼容不支持 BLE 的老机身；工程全压在网络发现 | 一旦相机不广播/路由隔离就没有第二把钥匙 | 我们的**降级路径**：AP 免蓝牙直连已在 v2.2 G2 落地（`AP_BLELESS`） |
| **C. 买第三方 SDK** | 像素蛋糕 | 最快上线、多品牌、免逆向维护 | 能力面受限于 SDK；**尼康并无面向第三方的移动端 SDK**，桌面授权库拿不到 | 结构上**不可复制**（尼康不发移动 SDK），这是我们的天然壁垒 |
| **D. 多端原生/跨平台** | 帧澈(五端)、影犀(Flutter) | 覆盖广、平台多 | 每端一套真机验证；**iOS 根本拿不到尼康 USB/PTP 控制**（帧澈自述需 Mac 桥接）；Flutter 的原生 USB/GATT 桥接层是稳定性黑洞 | 我们**主动选窄**：Android 单端 Kotlin 单栈。这是正确决策，别动摇 |

**决定性事实**（来自既有调研，反复验证）：尼康不向第三方提供移动端 SDK；Z 系列无 HTTP/Web Server 兜底（官方 WMU 明示「无法与兼容 SnapBridge 的相机配合使用」）；**机身同一时刻只允许 1 个 PTP/IP 客户端**。
→ 【推】**推论 1**：这条赛道的技术门槛是「逆向 + 真机矩阵」，不是架构；**推论 2**：机身单客户端约束意味着**我们和 SnapBridge 是互斥的**，谁的引导做得好谁赢；**推论 3**：任何声称「更简单方案（官方 SDK/HTTP）」的想法都是死路，不要回头再查。

---

## 5. N-Link 的定位与差异

### 5.1 一句话定位

> **面向尼康 Z 系列的开源 Android 连接与遥控工作台：把官方 App 做不到的（有线、原图批量、全量参数遥控、低延迟监看）做到可用、可诊断、可回退。**

### 5.2 三个结构性优势（对手短期难以复制）

1. **尼康 BLE 配对协议的 Android 原生实现 —— 全行业（含全部开源项目）唯一。**
   【实】`BleManager.kt:68` 四阶段配对 + `:939-949` `NikonBlowfish`（Blowfish/ECB/NoPadding）+ `NikonBlowfishTest.kt` 向量测试。帧澈搜 `blowfish/0x952B/_ptp._tcp` **0 命中**；ZRelay 闭源无法比对；SnapBridge 用 LsSec 加密且不外发。
2. **BLE + WiFi(AP/STA) + USB 三通道全量打通，且带统一诊断。**
   【实】12 Stage / 40 稳定原因码 `ConnFunnel` + 9 项 `PreflightGate` 预检 + `rom_rules.json` 机型引导。**ZDROP 无蓝牙、ZRelay 无 USB、帧澈无 BLE 配对** —— 三家各缺一角，我们全有。
3. **开源 + 纯本地（不上传照片）+ 双网盘分发。**
   在官方 App 有停摆先例、ZRelay 无商店上架被质疑隐私、帧澈「开源只在客户端（AI 是闭源云+兑换码）」的对照下，这是**唯一可被审计的可信性**。

> ⚠【推】2026-09-21 修订：**优势 1、2 的「Android」前提正在变化** —— 影控台 Android 版内测中（§2.11）。它 iOS 侧的完成度不代表 Android 侧能做到同样深（尼康私有配对在 Android 上仍是公开记录里的空白），但**「Android 上只有我们把尼康私有配对做穿了」这句话的有效期必须按季度复核**，不要当成永久优势写进对外文案。

### 5.3 三个结构性劣势（不要粉饰）

1. **监看是「够用」而不是「好用」**：15fps 硬编码 + 全 CPU 解码 + ImageView 上屏 + 仅亮度直方图，**没有一个监看工具（LUT/波形/峰值/斑马）**。帧澈有可调帧率 + 波形 + LUT；影控台（iOS）**LUT + 伪色 + 斑马 + 直方图 + 波形 + 安全区 + 水平仪 + 反挤压**一整套并据此分层收费 ¥148（§2.11）——**监看工具是「专业感」的来源，我们这块是空的**。
2. **拍摄能力缺包围与焦点包围，RAW 工作流缺配对策略**：包围/焦点包围全仓 0 命中；RAW+JPEG 配对下载未实现；RAW 分享副本代码里写着「本版未实现」。**风光/产品摄影的用户在这三项上会被帧澈直接拿走。**
3. **工程质量基建比竞品薄**：无 CI、无 instrumented 测试、无崩溃上报、无英文、兼容矩阵 19 条中 9 条从未真机验证、`proguard-rules.pro:4` keep 了一个**不存在的包**（`app.core.ptp` 实为 `app.device.ptp`）、targetSdk 落后一档、**仓库无 LICENSE 文件**。我们对外说「开源可信」，但可信性的基础设施本身有洞。

### 5.4 差异化叙事（对外怎么说，一页）

| 对手 | 我们比它强的那一句 |
|---|---|
| SnapBridge | 「原图 + NEF + 有线 + 遥控 + 监看，官方一样都没有；影像馆 10/15 还要关停。」 |
| 影速传 | 「它是商业传图前端，我们是拍摄工作台：B 门、间隔、包围、实时监看、参数全量读写。」 |
| ZDROP | 「它有网络工程没有蓝牙兜底；我们三通道 + 同一套诊断，且开源可审。」 |
| ZRelay | 「我们 USB 有线 + 包围之外全都有，且不发版也能改的兼容矩阵 + 商店之外的透明分发。」 |
| 帧澈 | 「同一件事 Android 单端做穿 > 五端做浅：BLE 尼康配对它有不了，真机验证它有『待实机验证』，我们每周发版。」 |
| 像素蛋糕 | 「不订阅、不上传、不依赖云；相机数据一个字节不出手机。」 |
| 影控台 | 「今天你在 Android 上装不到它；我们免费、开源、可审计，且三通道（BLE 配对 + AP/STA + 有线）在它没做的 Android 平台上已经跑通。**（此句有保质期，见 §5.2 ⚠）**」 |

---

## 6. 缺口清单（GAP → 交给 PRD 排期）

> 编号与 PRD 的 FR 一一对应。分三档：**地基债**（不做会掉用户）、**能力缺口**（竞品已验证的需求）、**战略空白**（决定半年后还在不在场）。

### 6.1 地基债（用户已高频抱怨，竞品已解决）

| ID | 缺口 | 证据 | 谁已解决 |
|---|---|---|---|
| GAP-01 | STA 侧存在**永久阻塞**的读取：非配对模式下 event 通道 `soTimeout = 0`，相机接了 TCP 却不回 `InitEventAck` 时整条连接挂死（阻塞 read 不是挂起点，上层 generation 校验根本轮不到执行）。**⚠ 本行原指 `StaNetworkRequester.kt:129`，2026-09-21 逐行复核后判定那条证据指错了地方** —— 该类的 `acquire()` 本来就有 deadline，只是没用带超时的三参 `requestNetwork` | 【实】`PtpSessionManager.kt:259`（`soTimeout = if (pairingMode) readTimeout else 0`）→ 卡在 `:269` 的 `fromStream`；连带 `StaNetworkRequester.kt:129` 两参重载、`WifiScanner.collectArp` 的 `timeoutMs` 声明未用、二段扫硬编码 18s。AP 侧对照：`wifi-ap/WifiManager.kt:147-153` | ZDROP/ZRelay |
| GAP-02 | 大库相册加载超时**整份丢弃**、增量回调三个入口全被关 | 【实】`TransferViewModel.kt:80,568,579`；`onPage=null` @ `:742,778` | 全部竞品（分页/流式） |
| GAP-03 | 下载队列串行并发度 = 1，批量吞吐受单流上限锁死 | 【实】`TransferManager.kt:1232` | ZDROP（双 data 通道）、aero-shutter（4MiB 分块） |
| GAP-04 | >2GB 只是「退出续传改整文件」，`offset: Int` 未升级、无 `PartialObject64` | 【实】`TransferManager.kt:747-757` | 影速传（官方支持大文件） |
| GAP-05 | **本行原判断已被证伪（2026-09-24 逐行复核）**：原文写「数据期不抽干 event 通道 → 丢加片/删除事件」，实际上 event 通道自会话建立起就有独立协程在持续读（`PtpSessionManager.kt:961` `startEventListener`，于 `:364` 启动），命令通道与事件通道是两条 socket，读取循环不碰 `commandMutex`/`thumbLane`，USB 侧同样有常驻轮询（`UsbPtpManager.kt:1117`）。**真缺陷在判死条件**：event 读超时一律 `markLinkError("event_read_timeout")`，而同样的机身静默在心跳路径是有 `bulkDepth` 豁免的（`:914-921`）——一处豁免一处不豁免，传大文件时等于自己掐线 | 【实】`PtpSessionManager.kt:961`/`:364`/`:914-921`/`:361` | ZDROP「双通道」的**真实收益是这一点**：静默期间不误判死，而不是"多一条读事件的腿" |
| GAP-06 | USB 通道无 `bulkDepth` 心跳豁免、无独立状态机（`ConnectionState` 无 USB 态） | 【实】`UsbPtpManager.kt:1093`；`ConnectionModels.kt:7-21`；`ConnectionManager.kt:738` 注释自认 | ZDROP（USB 同源处理） |
| GAP-07 | STA 主机注册固定 `Thread.sleep(8500)`、无回验 → 成功率不可解释 | 【实】`PtpProtocol.kt:120`、`PtpSessionManager.kt:400-428` | ZRelay（候选打分 + 拒绝原因） |
| GAP-08 | 兼容矩阵 19 条中 **9 条 `lastVerified: null`（从未真机验证）**；预检面板只有文案、**没有「去设置」深链落点** | 【实】`assets/compat/rom_rules.json`、`PreflightGate.kt` | ZRelay（服务端下发 + 持续验证） |
| GAP-09 | **零量化基线**：漏斗数据只在内存里存 12 次，无聚合、无崩溃率、无 p50/p95 连接耗时 | 【实】`ConnFunnel.kt`（内存 12 次）、崩溃仅本地 `crash.log` | 所有商业竞品 |

### 6.2 能力缺口（竞品已验证的需求，我们空白）

| ID | 缺口 | 证据 | 谁已解决 |
|---|---|---|---|
| GAP-10 | 监看 15fps 固定 + CPU 解码 + ImageView 上屏，无质量/帧率阶梯 | 【实】`LiveViewManager.kt:39,531`；`LiveViewFragment.kt:687-689`（`:131` 注释自证非 SurfaceView） | 帧澈（可调帧率）、影犀（Flutter 纹理） |
| GAP-11 | **监看工具补上了两件**：伪彩（12 档分档上色，画在取景叠加层里）与 **RGB 分量波形**（128 列 × 64 档三通道累加，保留画面位置）——PRD FR-13 两件均已落地，同一条闸门 `v25_tone_tools` 默认关，**均未经真机看效果与帧率**。仍空白：LUT、斑马纹、峰值对焦、矢量示波器 | 【实】`camera/liveview/PseudoColorLut.kt`、`RgbWaveform.kt`、`WaveformView.kt`、`ViewfinderOverlayView`；其余全仓 0 命中（直方图仍仅亮度 64 柱） | 帧澈（含矢量示波）、**影控台（LUT/伪色/斑马/直方图/波形/安全区/水平仪/反挤压，且据此收 ¥148）【官】** |
| GAP-12 | 无包围曝光、无焦点包围；机内延时属性 `0x5012` 声明未用（定时只有软件倒数） | 【实】全仓 0 命中；`PtpProtocol.kt` `PROP_CAPTURE_DELAY` | 帧澈、影犀、尼康官方桌面；**影控台通篇未宣称**（此需求在商业侧尚未被验证，慎排期） |
| GAP-13 | 无 RAW+JPEG 配对策略（只下 RAW / 只下 JPG / 配对下）；RAW 分享副本「本版未实现」。**2026-09-24：本行从"待排期"升为已进范围**——PRD 新增 FR-17 承接"成对显示 + 下载选择"这一半（不需要 NEF 解码），分享副本那一半仍不做 | 【实】`PreviewShareExporter.kt:74`；仅 `classifyFormat` 分类 | 帧澈（README 明列）、ZRelay、**影控台（1.1.0/1.2.0/1.3.1 三次迭代专门处理 RAW+JPEG 合并与下载选项【官】→ 同一件事被改三次，是高频痛点的强证据）** |
| GAP-14 | mDNS 只监听 2 种服务类型（影犀 9 种）；无 `_nikon-ptp`/`_nikon-pc` 等变体 | 【实】`WifiScanner.kt:88-89,960-961` | 影犀 |
| GAP-15 | **无 `TetheringManager`**：STA 场景必须用户手工开热点 + 相机手工连 | 【实】全仓 0 命中（v2.2 G4 未做） | ZRelay |
| GAP-16 | 触摸对焦无对焦框回显（候选码未标定，只打日志） | 【实】`AfReadback.kt:14` | 影犀/帧澈（回显） |
| GAP-17 | BLE 侧只做了配对/心跳：`0x2006` 写时间、`0x2007` 写位置、`0x2001/0x2009-0x200B` **全是 UUID 声明，零读写** | 【实】`BleManager.kt:41-51`，仅 `0x2002` 被读（`:609`）；v2.2 G24-26 未做 | SnapBridge（时间+位置）、ZRelay |
| GAP-18 | 兼容规则无热更新通道（竞品服务端下发） | 【实】v2.2 G21 未做 | ZRelay `/v1/camera-rules` |
| GAP-27 | **「相机侧标记 → 选择性传入」：读侧已做，写侧未做**（2026-09-24 更新）。保护位现在一路带到相册实体，并可按「已保护」一次筛出（PRD FR-16，闸门 `v25_protect_select` 默认关）；`0x1008` 与 `0x9805` 两条路径都填，**机身没报这一列时记为"未知"而不是"未保护"**。仍然不写：尼康机内那次「按保护键」我们读得到、替用户写不了 | 【实】`TransferManager.kt:869` → `CameraFile.protectionStatus`、`camera/gallery/ProtectionFilter.kt`、`MtpObjectPropListParserTest`；`PtpProtocol.kt:48-65` 操作码表仍止于 `0x101B`，**全仓无 `0x101A SetObjectProtectionStatus`**；【官】影控台 v2.0.0 速传 | 影控台（唯一） |
| GAP-28 | **无「无卡拍摄」**：影控台官方描述支持不插卡直落手机 | 【官】影控台商店页；我们依赖卡内对象枚举【实】。**能否落地取决于机身是否允许「无存储卡释放快门」，未真机验证** ❓ | 影控台 |

> GAP-27/28 为 2026-09-21 依 §2.11 增补（编号接全局，不按小节重置）。
> **GAP-27 是本次分析里性价比最高的一条**：它不需要私有命令、不需要云端、不需要后台常驻，只需要一个 PTP 公开操作码 + 一次 UI 语义改造，就把「用户想传哪几张」这个问题从手机侧搬到了相机侧——正是摄影师在拍摄现场的真实动作。

### 6.3 战略空白与合规风险

| ID | 事项 | 证据 | 风险等级 |
|---|---|---|---|
| GAP-19 | **`NikonLink` 重名**：`Tauber01/NikonLink` 永久重定向到帧澈，其 release 标题曾用「Nikon Link」，包名 `com.tauber.nikonlink` 明说永久保留；我们首提交晚 5 天，`applicationId=com.nikonlink.app` | 【实】GitHub 重定向 + 对方 README + 我们首提交 `2026-08-02` | **高**（GitHub 搜索权重/SEO 归零、用户装错包、Nikon 商标叠加） |
| GAP-20 | **无 LICENSE 文件**，README 自述「未附许可证，勿商用分发」 | 【实】`ls LICENSE` → MISSING | 高（「开源可信」的卖点不成立） |
| GAP-21 | targetSdk 35，竞品 ZRelay/影犀已到 **36**；无 16KB page size 相关配置（纯 JVM 依赖，风险低但未评估） | 【实】`build.gradle.kts:23`、全仓无 `useLegacyPackaging`/`android.aapt` | 中 |
| GAP-22 | **无 CI、无 instrumented 测试**；`ConnectionStateMachine` 单测 0 覆盖（核心状态机裸奔） | 【实】无 `.github/`、无 `app/src/androidTest`；17 个单测类 99 个 @Test | 中 |
| GAP-23 | 无英文：仅 `values/strings.xml` 38 条，Kotlin 内硬编码中文 **936 处**，`getString(R.string)` 仅 25 处 | 【实】res 目录无 `values-en` | 中（海外是唯一无强对手的市场） |
| GAP-24 | 文档卫生：README 引用 `docs/BLE-可行性评估.md`、`tools/README.md` 均不存在（死链） | 【实】文件 MISSING | 低 |
| GAP-25 | 技术待核：① 私有配对码的模式归属（我们 `0x952B/0x935A`(WMA 主机注册) vs ZTransfer `0x9838/0x93D4`(「Connect to computer」模式)）；② **我们只有 `0x90C7` CheckEvent、全仓无 `0x9714` GetEventEx，且注释把它误称为 GetEventEx**（注释已于 2026-09-24 订正为 CheckEvent + 指向 PRD FR-15②；**代码里的调用本身没变**，要验的是"只查不取"够不够）；③ `0x9A6C` 高速局部对象下载未探测 | 【实】`PtpProtocol.kt:96,115-121`、`PtpSessionManager.kt:350`【网】ZTransfer `PtpIpConstants.kt:52-63` | **高**（事件可能「只查不取」；下载速率可能白扔一半） |
| GAP-26 | iPad/横屏适配缺失（15 个布局全共享，无 `layout-sw600`/`layout-land`）；无障碍仅 68 处 contentDescription | 【实】res 目录 | 低-中 |

---

## 7. 更新动向与节奏对比

| 玩家 | 最近版本/日期 | 节奏 | 动向解读 |
|---|---|---|---|
| SnapBridge | 2.13.3 / 2026-01-06（iOS）；Android 国内停 2.12.0 | 一年 3–5 版 | **只做机型支持与修 bug，无功能演进**；影像馆 10/15 关停倒逼用户迁移 |
| 影速传 NX MobileAir | Android 1.6.0+ | 慢 | **专业版免费解锁相册/保存** = 官方开始抢第三方饭碗 |
| ZDROP | 1.0.257 | 高频小版 | 版本尾部持续迭代，重心在连接与诊断 |
| ZRelay | 3.0.46 | 高频；3.0 加光圈/ISO/WB/快门/监看 | **从传图工具升级为遥控工作台**，正撞进我们的腹地 |
| 影犀 | 1.2.1，target 36 | 中 | 多品牌横向铺开，LUT/水印是卖点 |
| 像素蛋糕 | 1.9.2 / 2026-09-14 | 商业节奏 | 订阅变现，联机是 AI 修图的入口 |
| 帧澈 ZENCHE | v1.5.15 / **2026-08-22 后停更（09-24 已 33 天）** | 曾 1.6 天/版 | **广度铺开但已失速**；AI 走闭源云付费 |
| ZTransfer | v1.0.2 / 2026-08-21 | **⚠ 09-24 复核：已停更 34 天** | 同细分、Kotlin、拿到私有配对码；旧版本表写「威胁增速最快」，**经 GitHub API 复核不成立**（★6、0 open issue、34 天无提交）→ 降级为「技术线索来源」而非速度威胁 |
| **影控台** | iOS 2.2.0 / 2026-09-10（Lite 同版同日） | **10 周可数 21 版 ≈ 3 天一版**【官】 | **能力面扩张最快**：7/2 首发 → 9/10 已有速传、群组、伪彩/斑马/波形、水印、巧摄接线、PZ 控制；**Android 内测中【用】** |
| **N-Link** | v2.3.1 / 2026-09-21（versionCode 23，tag `v2.3.1`） | **22 个 tag / 08-17→09-21 共 36 天 ≈ 1.6 天一版**【实 `git tag`】 | 节奏仍是最快档；但**发版速度已从「差异点」降为「入场券」**（影控台 3 天一版、changelog 一样细），真机验证与量化基线仍滞后于发版速度（GAP-08/09/22） |

**关键洞察【推】**：**发版速度不再是我们的差异化**——旧版本文写「我们节奏全场第一」，数字上也确实更快（1.6 天/版 vs 影控台 3 天/版），但**同一个量级**，而影控台把节奏兑换成了 App Store 上的评分与公开版本史。真正拉开差距的是**可证明性**：它有 4.6/5（55 评）与逐版 changelog 作为信任凭证，我们只有 GitHub 提交记录和网盘链接，普通用户搜不到也验不了；同时我们**没有 p50/p95 连接耗时、没有首连成功率、没有崩溃率**。竞品（ZRelay）已在用服务端兼容规则持续收敛，帧澈已停更。**下一个 3 个月，谁的「可证明的稳定」跑出来，谁就拿走这条赛道的话语权。** 因此 PRD 的优先级必须是：**基线与地基债 > 监看工具 > 拍摄补齐 > 战略空白**，而不是继续堆新功能。

### 7.1 更新动向复核（2026-09-24，一手 API 取证）

> 复核手段：`gh api repos/{owner}/{repo}` + `releases/latest`（GitHub 一手），`itunes.apple.com/cn/lookup`（App Store 一手：版本号、发布日期、评分、评分数）。**只记数字变化，不改结论，除非结论被推翻。**

| 对手 | 09-21 记录 | 09-24 复核值 | 变化与含义 |
|---|---|---|---|
| **影控台**（iOS `id6768196167`） | v2.2.0 / 2026-09-10，4.6/55 评 | **v2.3.1 / 2026-09-23，4.63/57 评** | **唯一一个仍在推进的商用对手**：13 天内 2.2.0→2.3.1（约 2 版，仍领先其自身"3 天一版"口径），评分数 3 天 +2。§2.11 的「能力面扩张最快」**结论上调**，且其 Android 内测若如期落地，窗口期以**周**计不以季度计 |
| SnapBridge（iOS `id1121563450`） | 2.13.3 / 2026-01-06，4.7/18428 | **2.13.3 未变**，4.714/18432 | **8 个月无版本**；3 周仅 +4 条评分 = 中国区讨论量仍在但增长极缓。官方停滞=第三方空间，与 §2.1 判断一致 |
| 帧澈 ZENCHE | 停更 29 天，★125 | **push 仍为 2026-08-22（33 天）**，★126，2 open issue | 停更持续，「五端叙事失速」成立；+1★/3天说明**关注度也在冷却** |
| ZTransfer | 「威胁增速最快」 | **push 仍为 2026-08-21（34 天）**，★6，0 open issue | ⚠ **原判断被推翻**：它没有"增速"，与帧澈同处停更态。已在上表与 §2.8 降级为**技术线索来源**（其私有 op 表仍值得自行探测复核，见 GAP-25 / PRD FR-15） |
| aero-shutter | v0.8.8 / 2026-07-24 | 未变（62 天），★5 | 参考件，非对手 |
| libgphoto2 | v2.5.34 / 2026-05-30 | push 2026-09-20（活跃），★1380，**479 open issue** | 桌面存量仍在维护，Z 系列取景/联网能力仍无进展（issue #1135 `PTP error 2005` 未解）→ 印证「桌面库不能直接搬进 Android」的路线判断 |

**本轮复核只改一个结论**：把 ZTransfer 从「速度威胁」降为「技术线索」，把影控台从「iOS 侧的能力参照」升为**本期唯一仍在扩张的商业对手**。
**由此得到一个更硬的时间约束【推】**：开源同路人集体停更（帧澈 33 天、ZTransfer 34 天、aero-shutter 62 天），官方 8 个月不动，**赛道的节奏压力全部来自影控台向 Android 的那一步**。我们 PRD §七 原本的锚点是"卡在影像馆停服（2026-10-15）之前"，**2026-09-24 用户裁定不要这个外部硬日期，锚点已改为本文这条**：参照系不是"还要不要再堆功能"，而是**它那一步落地之前，我们有没有把可证明的稳定做出来**。

---

## 8. 机会窗口（3–6 个月）

1. **影像馆关停（2026-10-15）** → 官方云备份用户被迫迁移，「本地 + 网盘 + 不上传」叙事窗口，**时间敏感**。
2. **帧澈停更 33 天（至 2026-09-24）** → 开源阵营的「五端叙事」正在失速，我们若补齐监看工具 + 包围/焦点包围 + RAW 配对，就能在**同一批需求**上形成「单端做穿」的对照。（⚠ 2026-09-21 订正：本条原文写的「iOS 无法直连尼康」**是错的** —— 影控台已在 iOS 上做穿直连，见 §2.11。）
3. **ZRelay 无 USB、无商店上架** → 「有线 + 可审计 + 开源」的组合它短期补不上；它 3.0 刚进遥控，正是拉开身位的时候。
4. **影速传免费化** → 正面冲突已开始，**必须**在对内文案与 App 内引导里把差异说清楚，否则用户会问「官方都免费了为什么还要装你」。
5. **机身单 PTP/IP 客户端约束** → 官方 App 用户量越大，「和 SnapBridge 冲突」的抱怨越多。**做一个「检测到占用 → 一键引导腾挪」的功能，是竞品都没有的、只有我们能做的、且直接消解最高频差评的点。**（→ PRD FR-07；编号已按 v2.5 定稿校正，原文误写 FR-14）
6. **`0x9A6C` 高速局部对象下载**（若真机成立）→ 全场唯一的速率跃升机会，且开源界无人做到。
7. **影控台的 Android 版还在内测**（§2.11）→ 这是**我们唯一确定还在、且注定消失的窗口**：Android 侧「尼康私有语义做穿 + 三通道 + 开源可审计」今天无人占位。按它的版本节奏推断窗口以**月**计，不按季度计。含义很直接：**v2.5 的重点不该是再多堆功能，而是把「可证明的稳定」立起来**（基线、真机验证、可被搜到的信任凭证），等它进场时我们手里要有别人拿不出的证据。

---

## 附录 A：本地取证产物索引

| 文件 | 内容 |
|---|---|
| `_apk_re/badging/<App>.manifest.txt` | 竞品 Manifest 解析（权限/组件/SDK 版本） |
| `_apk_re/dex/classes_*.txt` | DEX 类清单（协议类、服务、包结构） |
| `_apk_re/scan/<APP>.txt` / `<APP>_cjk.txt` / `YINGXI_dart.txt` | 字符串与中文文案扫描（**竞品日志文案**） |
| `_apk_re/so/` | 原生库导出符号（`libcamerawirelesscontrol.so` 111 符号等） |
| `_apk_re/zdrop_hostreg_disasm.txt` | ZDROP 主机注册流程反汇编（`0x952B/0x935A` 出处） |
| `docs/archive/PRD-N-Link-v2.2-连接三通道稳定性与方案选型.md` | G1–G26 改进项与竞品对标的原始详版（§二、§10）；**2026-09-21 起历史 PRD 统一归档在 `docs/archive/`** |
| `.workbuddy/memory/2026-09-06.md` | 四款 APK 实测（LUT 数量/版权、体积、栈）与 SnapBridge 口碑口径 |
| App Store 中国页 `id6768196167` / `id6788416417` | 影控台 / 影控台 Lite 商店页（**2026-09-21 取，含完整版本历史**）—— §2.11 的唯一来源，非逆向 |

## 附录 B：待核实清单（不要当成结论用）

- ~~【网】影控台 ¥148~~ → **已于 2026-09-21 用 App Store 中国页复核为【官】**（¥148 买断 + Lite 免费 + Lite Plus ¥9.9/月·¥68/年），从本清单移除。ZRelay 定价 ¥20 Pro、Zensō ¥68、ZLinkLab ¥9.9 仍来自 smzdm 系 AI 聚合稿，**须以商店页/APK 复核**。
- 【用】**影控台 Android 内测的四件事全部未知，且直接决定我们的窗口长度**：① 功能面是否与 iOS 等价（还是先做传图、监看工具后置）；② 是否沿用「Expeed 代际分层 + 买断/订阅」这套商业结构；③ 分发渠道（Google Play 在国内不可用 → 大概率又是私域包）；④ **它有没有做尼康 BLE 私有配对**（iOS 描述未提蓝牙；若 Android 版靠 BLE 配对才好用，那它反而撞进我们的强项区）。**这四项由用户在内测群里实地确认，优先级高于本文任何一条待核。**
- 【网】SnapBridge「1.9/5、1 星占 64%」是第三方聚合口径，与苹果商店 4.7 冲突；**引用时须并列两个口径**。
- 【网】「2026-05 尼康给 Z50II 悄悄更新固件、用户需先断联再重连」—— 标题可查、正文未取证；「新固件破坏第三方配对」目前**无公开证据**，但 2025-06 固件确实改过配对时的相机显示名，**足以打断按设备名匹配的实现** → 列入真机回归矩阵。
- 【推】GAP-25 的两个配对码可能是**两条并存通路**（WMA 主机注册 vs 无线连接计算机），而非谁对谁错；探测前不要在文档里写「我们用的是错的」。
